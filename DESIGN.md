# Design

<!-- impeccable:design-schema 1 -->

이 프로젝트는 **두 개의 비주얼 register**를 가진다.

1. **앱 코어(Operate) — 「오직 트레이너」 Design System.** 밝은테마 실사용 화면(트레이너 앱·회원앱·관리자). 토큰 정본은 [app/globals.css](app/globals.css) `@theme`, 프리미티브는 `components/ui/`. 상세 규율은 [CLAUDE.md](CLAUDE.md) "디자인 시스템 프리미티브". **이 문서는 그걸 대체하지 않는다** — Operate 화면은 계속 그 DS를 따른다.
2. **랜딩(Persuade) — 정공법(클린 컨벤셔널).** `/lp` 마케팅·유입 표면. 아래에 정의. 세계관 2회(스코어보드·코칭 노트) 폐기 후, 사용자 지시로 **표준 랜딩 + "AI 티" 배제**. 랜딩 전용 CSS 모듈 없이 브랜드 `@theme` 토큰 위 유틸만.

---

## 브랜드 방향 요약 — 주조색·타이포·금지 (경쟁 차별 · 2026-07-28)

> 이미 확정된 정체성(레드 + Pretendard)의 **정식 근거**. 세부 토큰은 아래 "브랜드 토대", 랜딩 확장은 "랜딩 월드" 참조.

### 색 (3역할 · 각 이유)

| 역할 | 값 | 왜 이 색인가 (경쟁 차별) |
|---|---|---|
| 주조색 | 레드 `#dc2626` (글자 `#b91c1c`) | 피트니스 앱이 네온 라임·에너지 그린·블랙에 몰릴 때, **절제된 '신호 레드'**로 "레드=누르는 곳(행동)" 역할 규율. 흥분이 아니라 **결단**의 색. |
| 배경 | 라이트 `#f1f2f6` / 아레나 `#eef0f5` | 헬스장 조명·직사광에서 다크는 반사로 안 보임 → **밝은테마 전용**이 다크 일색 경쟁 피트니스 앱과 정반대 포지션. |
| 본문 | 잉크 `#13151b` (푸른기 뺀 중립 먹색) | 순 `#000`의 눈부심 없이 **4.5:1+ 고대비** — '3분 컨닝페이퍼'를 밝은 화면에서 빠르게 읽는 가독 우선. |

### 타이포 (Inter·Roboto·시스템 제외)

- **제목용 — Pretendard Heavy (800–920 · 자간 −0.03em):** 한글 글리프를 한 벌로 통제(기기별 조판 불일치 제거), 가변축 900 near-black으로 기념비적 헤드라인. 경쟁 앱이 시스템 한글 폴백으로 흔들릴 때 통제권 확보.
- **본문용 — Pretendard Regular (450–500):** 위계는 서체 교체가 아니라 **굵기·자간**으로 — 일관성 + 로딩 최소(헬스장 회선). 디스플레이 세리프 섞기(=AI 랜딩 클리셰)는 의도적 배제.
- **데이터/숫자 — Geist Mono (tabular):** 스탯·금액 리드아웃 전용(라틴). 손글씨 `--font-handwriting`은 세일즈북 전용.

### 절대 금지 3가지 (경쟁·클리셰 회피)

1. **초록으로 성과·금액 표시** — "돈=초록" 관습 금지(색=역할 규율 위반 + 재무 초록은 경쟁 대시보드 클리셰). 매출도 잉크/모노로.
2. **허위 긴급성·카운트다운 압박 연출** — "지금 안 하면 손해" 세일즈 몰이 금지(제품 윤리 = 재등록은 만족의 결과, 강매 아님).
3. **근육·땀 사진 + 네온/에너지드링크 톤**(피트니스 러트) **및** 그 반대 극단인 **SaaS 그라디언트+목업 3개 러트** — 증명은 실제 앱 화면·방송 그래픽으로.

---

## 브랜드 토대 (두 register 공통 · 불변)

- **이름/마크:** 오직 트레이너 / ONLY FOR TRAINER. `Wordmark`("오직"=ink·"트레이너"=primary)·`BrandMark`(SVG 심볼). 랜딩도 이 마크를 그대로 쓴다.
- **브랜드 레드:** `--color-primary #dc2626`(채움·CTA·LIVE) / `--color-primary-strong #b91c1c`(밝은 배경 위 글자). **레드는 절제** — 누르는 곳/온에어 신호에만.
- **서체:** Pretendard 한 벌(가변 45–920, 한글·라틴). `font-mono`(Geist Mono)는 숫자·스탯 전용. `--font-handwriting`(나눔펜)은 세일즈북 전용. **외부 폰트 추가 금지**(헬스장 회선 · self-host 규율).
- **색은 역할:** 레드 채움=누르는 곳, 레드 텍스트=보는 곳. **금액을 초록으로 칠하지 않는다.**
- **역할색(업무흐름):** ot=amber `#b45309`(-text) · pt=sky `#0369a1` · admin=fuchsia `#a21caf` · danger=rose. 밝은 배경 위 글자는 반드시 `-text`.
- **아이콘:** lucide-react만.
- **접근성:** 본문 대비 **4.5:1 이상**(배경 3종 모두에서 측정). 색은 정보의 유일 수단 아님(색+라벨 병행). `prefers-reduced-motion` 존중.

---

## 랜딩 — 정공법(클린 컨벤셔널) (Persuade · `/lp`)

> ⚠️ **세계관 2회 폐기 후 정공법 채택**(2026-07-29). 시도한 "라이브 중계 스코어보드"·"코칭 노트"는 모두 사용자 판단으로 폐기. 사용자 지시 = **일반 랜딩페이지 + "AI 티" 배제**. 구조·카피·3막 흐름은 유지. 이건 표준 랜딩을 **정공법으로 풀 피델리티** 실행한 것(아이러니·숨은 quirk 없음).

### 스타일 방식 — 브랜드 토큰 위 Tailwind 유틸만

**랜딩 전용 CSS 모듈 없음.** 전역 `@theme` 브랜드 토큰(`bg`/`card`/`elevate`/`ink`/`sub`/`muted`/`line`/`line-strong`/`primary`/`primary-strong`)을 그대로 쓴다. `app/lp/page.jsx`가 유틸 클래스 + 짧은 상수(`BTN`/`FIELD`/`CARD`)로 구성. `DemoSlot`·`Reveal`·`StatCounter`만 세계 중립 유틸로 계승.

- **지면:** 흰색(`bg-card`)과 연회색(`bg-bg #f1f2f6`) 섹션 **교차** · 구분선 `border-line`. 배경은 전부 뉴트럴(채도 0).
- **단일 액센트 = 브랜드 레드** `primary #dc2626` / 글자 `primary-strong #b91c1c`(주 CTA·아이콘·eyebrow·번호). **절제** — 레드는 행동/강조에만.
- **본문** `ink #13151b` / 보조 `sub #475569` / 흐림 `muted #5b6675`. 흰·연회색 위에서만(채도 있는 배경 위 회색 본문 없음).
- **서체:** Pretendard(헤드라인 `font-black`, 본문 regular) + Geist Mono(수치 `tabular-nums`)만.
- **카드:** `CARD = rounded-2xl border border-line bg-card p-6 shadow-sm` **단일 레벨**(카드 안 카드 없음).
- **스크린샷:** 깔끔한 프레임(`Frame` — 테두리 + 라벨 바, 가짜 브라우저 크롬·기울기 없음)에 **실제 앱 화면**(iframe). 기능부 = 스크린샷+카피 교차 행(표준).
- **버튼:** `BTN_PRIMARY`(레드 채움) / `BTN_GHOST`(라인 아웃라인). 프레스 `active:translate-y-px`(바운스 아님).

### AI-슬롭 배제 규율 (이 표면의 핵심 지시)

- **그라데이션 0**(보라·파랑 포함 어떤 그라데도) · 글로우·글래스·네온 0.
- **generic 3-목업 히어로·아이콘 타일 feature 그리드 금지** → 실제 스크린샷 + 카피 행으로.
- 크림+세리프, 다크+네온 등 AI 클러스터 룩 회피. 이모지 0. 모호한 카피 0(전부 구체).
- **사용자 5대 금지 엄수:** Inter/Roboto/시스템 폰트 · 보라/파랑 그라데 배경 · 카드 안 카드 · 색 있는 배경 위 회색 본문 · 바운스.
- **브랜드 규율:** 초록으로 성과/금액 금지 · 허위 긴급성 금지(긴급성은 진짜 이유만).

### 검증됨 (2026-07-29 · 클린 빌드)

- 그라데이션 0 · 폰트 Pretendard/Geist Mono만 · 카드-인-카드 0 · 바운스 이징 0 · 가로 오버플로우 0 · 콘솔 에러 0 · 실제 데모 7개.
- 포커스: 커스텀 컨트롤에 `focus-visible:ring-2 ring-primary`.
- ⚠️ 이전 두 세계(`broadcast.module.css`·`notebook.module.css`)는 삭제 — git 히스토리 참고.

### 남은 follow-up (코드 밖)

- **사전신청 제출 엔드포인트** — 현재 클라 성공 상태 + `localStorage`. 실제 저장(Supabase `waitlist` 등)으로 POST 연결은 사용자 교체 대상.
- 파트너 로고·후기·성과 수치는 베타라 미포함(조작 금지) — 실제 확보 시 추가.
