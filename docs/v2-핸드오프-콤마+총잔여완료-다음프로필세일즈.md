# v2 인계 — §2 마감 + #3 콤마입력 + #1 총/잔여수업 완료 → 다음 #4 프로필 맞춤세일즈

> 이 문서 = 새 채팅 이어가기용 인계(제일 최신). 함께 첨부(권장): **이 문서** · CLAUDE.md · docs/MASTERPLAN.md · docs/pt-navigator-총정리.md · 직전 인계(`v2-핸드오프-solo가입+앱느낌완료-다음기능백로그.md`).
> 역할·흐름: 웹Claude(스펙·검토)→트레이너→클로드코드(diff)→트레이너(git diff)→웹Claude(검토)→트레이너(폰).
> 커밋은 트레이너 직접(파일지정 `git add` · lint 통과 후 · 직후 `git show --stat HEAD`). SQL=Supabase 대시보드, git엔 `docs/migrations` 기록본.
> 환경: PowerShell(C:\Users\tig00\pt-navigator · grep 없음 → `Select-String`, 재귀는 `Get-ChildItem -Recurse | Select-String`). lint=`npm.cmd run lint`. 폰=Vercel 배포본 하드리프레시. 배포본 URL=`https://pt-navigator.vercel.app`.

---

## 0. 이번 세션 성과 (전부 검증·커밋 완료)

### A. §2 남은 확인 마감 (직전 인계의 미결 3종 — 전부 종결)
- **service_role 감사(코드변경 0):** PowerShell 5개 전부 클린. `SERVICE_ROLE`는 `app/api/create-trainer/route.js:14`(`process.env.SUPABASE_SERVICE_ROLE_KEY`) **1곳뿐**, `NEXT_PUBLIC.*SERVICE` 없음, `console.*` 키 로깅 없음, `.env.local` git-ignored·히스토리 미커밋. → 로테이트 불필요.
  - ⚠️ 핸드오프 원본의 `Select-String -Path <dir>`는 PowerShell에서 디렉터리 재귀 안 됨 → **`Get-ChildItem app,components,lib,scripts,hooks -Recurse -File | Select-String`** 로 돌려야 정확(빈 결과 오판 방지).
- **오프보딩 active SQL 적용:** `docs/migrations/2026-07-13-offboarding-active.sql`(헬퍼 2개 `and active`) 실행 완료. 사전확인 `select id,role,active from trainer;` 전부 `true`, `update trainer set active=true where active is null;`도 실행(NULL 청소).
- **셀프 가입 테스트 3종 통과:** 트리거 SQL(`2026-07-13-signup-trigger.sql`) 적용 + Authentication → Email **"Confirm email" OFF** 후:
  - 개인(solo): `개인테스트`(tig00540+solo@gmail.com) → account(solo)+owner. **'관리자' 링크 숨김** 확인.
  - 센터(center): `센터원장테스트`(tig00540+center@gmail.com, 센터명 `테스트센터`) → account(center)+owner. **관리자 링크 노출 + admin 헤더 "테스트센터"** 확인.
  - 초대(create-trainer): `옹트레이너`가 **테스트센터 account 밑 role=trainer**(새 account 안 생김) → 비충돌 실증.
  - `Database error saving new user` 없음(트리거 NOT NULL 문제 없음).

### B. #3 숫자입력 콤마 (커밋·배포 스모크 완료)
- **신규 `components/ui/NumberInput.jsx`** — `type="text"`+`inputMode="numeric"`, **value=콤마 없는 정수 문자열(부모 state 그대로), 화면에만 콤마**. 부모 계약: `value`(문자열)·`onValueChange`(raw문자열). 캐럿 보존(requestAnimationFrame). **정수 전용**(소수·% 금지).
- **7파일 치환**(type="number"·inputMode·onChange 제거 → value/onValueChange, className·placeholder·disabled 유지):
  `ContractAmountFields`(4칸) · `PtPricingSettings`(세션수·실판매가·정가 — **sort 제외**) · `RefundMember` · `PayrollConfirm` · `TrainerGoalSetter` · `AdminPayrollSettings`(밴드min·예시3칸 — **지급값·인센값 제외**) · `app/page.jsx` MemberForm 이월 2칸(carrySessions·carryPrice, `set()`→`setForm` 인라인).
- **제외(그대로 type=number):** 지급%·인센%(퍼센트) · PtInbodyTab(소수 kg) · 나이 · sort. → 소수/퍼센트/식별용은 콤마 금지.
- **배포 스모크 통과:** 목표매출·실판매가 콤마 실시간(`12,000,000`/`1,200,000`), 저장 왕복 시 DB 정수(`won()` 정상), 새로고침 프리필 정수→콤마.
- 스펙: `docs/v2-스펙-콤마입력-숫자입력컴포넌트.md`.

### C. #1 총수업수·잔여수업수 (커밋 완료 · **트레이너 집계판**)
- **정의(트레이너 확정):** 회원 1명 카드 아님 → **내 활성 PT 회원(status `pt_active`) 전원의 계약 세션 합**. 총 = Σ(`sessions_total`+`service_sessions`, **서비스 포함**, 신규+재등록+인계 전부), 잔여 = Σ `remainingSessions().total`, 완료 = 총 − 잔여. **inactive(PT 끝난)·OT·남의 회원 제외.**
- **위치·구현:** `components/views/MyStats.jsx` **1파일**. MyStats가 이미 본인 전 계약(session_log)·전 로그(daily_workout_log)·members 로드 중이라 **추가 쿼리 0**. import에 `remainingSessions`·`viewFor` 추가 → 파생(`ptMemberIds`/`ptContracts`/`sessTotalAll`/`remAll`/`doneAll`) → '이번달 수업' details 앞에 **"PT 수업 현황" 카드**(잔여 N회 + 완료 X/총 Y + 유료·서비스 + 진행 바, `ptMemberIds.size>0`일 때만).
- `memberStatus.js` 무변경. 기존 매출/급여/클로징 집계 무영향.
- ⚠️ **배포 스모크 미실시** — 테스트 계정(개인테스트)엔 PT 회원 0이라 카드 숨김. **실계정(파일럿, PT 회원 有)에서 폰 확인 권장**(§3 손검산: 회원 몇 명 계약합·비-voided 로그수 눈검산 일치).
- 스펙: `docs/v2-스펙-총잔여수업표시.md`.

---

## 1. 커밋 메시지 (제안 · 트레이너가 친 것 참고)
- `feat(ui): 천단위 콤마 숫자입력(NumberInput) — 가격·세션·급여·환불·이월 7파일`
- `feat(mystats): PT 수업 현황 카드 — 활성 PT 회원 전체 총/잔여 수업 합`
- (§2는 코드변경 0 — 대시보드 SQL/토글만, 커밋 없음)

---

## 2. 남은 즉시 확인 / 미결
- [ ] **#1 실계정 배포 스모크** — PT 회원 있는 계정 내 실적 탭에서 'PT 수업 현황' 숫자 손검산.
- [ ] **런칭 전 Email "Confirm email" 다시 ON**(테스트로 OFF 해둠).
- [ ] **테스트 계정 teardown**(런칭 전): `개인테스트`(+solo · trainer_goal 목표 12,000,000 저장돼 있음) · `테스트센터`+`옹트레이너`(+center) — trainer/account/session_log 삭제 + Authentication 유저 삭제.

---

## 3. 다음 = 기능 백로그 (이 순서로)

| 순 | # | 요청 | 크기 | 착수 시 |
|---|---|---|---|---|
| **다음** | **4** | **트레이너 프로필 → 맞춤 세일즈** | M | **1·2·재등록 세 phase 전부**(급한불 제외 — 의료안전). `app/api/ot-brief`의 `firstPrompt`/`secondPrompt`/`reregisterPrompt`에 트레이너 프로필 블록 주입 + 클라 호출부 3곳(`FirstOTAssist`·`SecondOTTab`·`PtReRegTab`)이 프로필 전송. **착수 전 `TrainerProfileSettings.jsx` + `docs/migrations/2026-07-12-trainer-profile.sql` + `app/api/ot-brief/route.js`(PREAMBLE·각 phase 프롬프트) 읽기.** 프로필 필드=자신있는 방향(다중)·선호 세일즈 강도(soft/standard/strong)·MBTI·한줄소개(SettingsView '내 프로필'에서 이미 입력·저장 중). |
| | 5 | 1차 지원 캐시+재생성 금지+확인 | M | 지금 1차(`FirstOTAssist`)는 무캐시·무한재생성(비용). 캐시 위치(ot_log round1 report?)·"회원정보 맞나?" 확인 UX **결정 필요**. |
| | 6 | 관찰기록 입력 간략화 | M | `ObservationTab`(큰 폼) 읽고 뭘 줄일지 제안(웹Claude 판단). |
| | 2 | OT도 음성일지 | M | **의미 미정** — OT엔 계약·수업차감 없음. "OT 수업 구두요약→관찰기록 채우기"인지 트레이너 확인 필요. |

- 의존성 없음(4·5 둘 다 1차 근처지만 별개: 4=세일즈3종 프로필주입, 5=1차만 캐시/확인).

---

## 4. 이번 세션에서 굳어진 규약(다음에 참고)
- **NumberInput 계약:** `value`=콤마 없는 정수 문자열, `onValueChange`=(raw)=>void. 새 돈/정수 입력 만들 땐 이거 재사용. **소수·퍼센트엔 쓰지 말 것**(toRaw가 소수점 지움).
- **MyStats 데이터:** 마운트 1회에 본인 전 계약·전 로그·members·pay_scheme·payroll_run·ot_log·trainer_goal 다 로드 → 트레이너 집계는 여기서 추가쿼리 없이 파생 가능.
- **PowerShell 재귀 grep:** `Get-ChildItem <dirs> -Recurse -File | Select-String -Pattern '...'`.
- **클로드코드 레일:** 스펙 §grep로 완전성 대조 → 불확실하면 멈추고 질문(이번에 MemberForm 이월 2칸을 이렇게 잡음) → 파일지정 add · `git add .` 금지 · lint 후 커밋 · `git show --stat HEAD`.

---

## 5. 관련 스펙/마이그레이션 문서 (docs/)
- 이번 신규: `v2-스펙-콤마입력-숫자입력컴포넌트.md` · `v2-스펙-총잔여수업표시.md`
- §2 근거: `v2-스펙-보안즉시묶음-API인증+active+service_role.md` · `migrations/2026-07-13-offboarding-active.sql` · `migrations/2026-07-13-signup-trigger.sql` · `v2-스펙-solo-PhaseA/PhaseB`
- #4 착수용: `migrations/2026-07-12-trainer-profile.sql` · `v2-트레이너-스펙-목표매출+프로필.md`

---

## 6. 새 채팅 첫 메시지 (복붙용)

> pt-navigator 트레이너야. 역할=나 클로드코드 코딩, 너(웹Claude) 설계·스펙·검토. 흐름: 너(스펙)→나→클로드코드(diff)→나(git diff)→너(검토)→나(폰). 커밋은 내가 직접(파일지정 git add · lint 통과 후 · 직후 `git show --stat HEAD`).
> 환경: PowerShell(C:\Users\tig00\pt-navigator · grep 없음→Select-String, 재귀는 Get-ChildItem -Recurse | Select-String). lint=`npm.cmd run lint`. SQL=Supabase 대시보드(git엔 docs/migrations 기록본). 폰=Vercel 배포본(`https://pt-navigator.vercel.app`) 하드리프레시.
> 첨부 먼저 읽어줘: **이 문서(v2-핸드오프-콤마+총잔여완료-다음프로필세일즈.md · 제일 최신)** + CLAUDE.md + docs/MASTERPLAN.md + docs/pt-navigator-총정리.md.
> 상태: §2(보안 감사·오프보딩·solo 셀프가입 3종) 마감 + #3 콤마입력(NumberInput 7파일·배포검증) + #1 총/잔여수업(내 실적 PT 수업 현황 카드) **완료**. 다음 = **#4 트레이너 프로필 → 맞춤 세일즈(1·2·재등록 3 phase)**. 착수 전 `TrainerProfileSettings.jsx` + `docs/migrations/2026-07-12-trainer-profile.sql` + `app/api/ot-brief/route.js`(PREAMBLE·firstPrompt·secondPrompt·reregisterPrompt) 읽고 스펙 잡아줘. (미결: #1 실계정 스모크 · 런칭 전 Confirm email ON · 테스트계정 teardown.)
