# 인계서(캐치업) — 오직 트레이너(pt-navigator) · 2026-07-21
> **범위:** 직전 핸드오프(`v2-핸드오프-2026-07-19-P0보안+관측성+가드완료.md`, 커밋 `155abc6`) **이후 ~ 현재 HEAD `ffc6370`** 까지 **55커밋**을 사후 정리한 캐치업 문서다. 여러 세션(07-19 저녁 · 07-20 오운완 · 07-21 디자인/성능)이 핸드오프 없이 누적돼 이걸로 한 번에 마감한다.
> 새 채팅 맨 위에 이 파일을 붙여넣고 이어간다.
> ⚠️ **먼저 확인:** `git log --oneline -12` + `git status`.
>  - push 상태는 이 문서 작성 시점 기준 **깨끗함**: 로컬 `main` = `origin/main` = **`ffc6370`**(P0-6). ahead 0.
>  - ⚠️ **단, 미add 새 파일 확인 필수** — 이번 구간 디자인 개편에서 신규 파일이 다수 생겼다(`Field.jsx`·`Card.jsx`·`AIBriefBlock.jsx`·`Modal.jsx`·`BrandMark`·`lib/fetchAllRows.js`·`ONLY FOR TRAINER/` 미러). `git status`로 untracked 잔재가 없는지 한 번 훑을 것.
>  - ⚠️ **이 문서는 커밋 메시지 55건으로 재구성**했다(웹Claude가 각 diff를 직접 읽고 쓴 게 아님). 세부가 중요하면 해당 커밋 diff로 대조할 것. 완료 판정은 "커밋 존재" 기준이지 재검증 기준이 아니다.

## 협업 방식 (그대로 유지)
- **웹Claude(너):** 수정 스펙 작성 + 코드 리뷰
- **클로드코드:** 실제 코딩(로컬 Next.js 레포)
- **나(대수):** git diff 붙여넣기 → 너가 리뷰 → **커밋은 내가 직접**
- **제약:** 로직·RLS·payload 불변이 기본(의도 변경은 명시). 표시/UI·additive 우선. 스펙은 **라인번호 말고 코드 내용으로 매칭**. 새 파일은 `git add` 먼저.
- 환경: 윈도우 PowerShell — `&&` 안 됨. 빌드 `npm.cmd run build`, 린트 `npm.cmd run lint`, npx는 `npx.cmd`. 커밋은 `git commit -m "..." -- <경로>`로 범위 한정(배치별 1커밋 = revert 단위).

## 스택·규칙 요약 (이번 구간 갱신 반영)
- Next.js 16(Turbopack) · Tailwind v4(밝은 테마 + 레드 `@theme`) · Supabase(풀 RLS·account 스코프 `auth_account_id()` + 구독게이트 `my_account_status`·`auth_account_plan` · 교훈1 하드닝) · Vercel Hobby + fluid compute(300s). AI 라우트 `maxDuration=180`.
- **🆕 디자인 시스템(DS)이 표준이 됨(07-21 2단계 전면개편):** Pretendard 폰트 + 「유일침」 로고/워드마크 2색(오직=`ink`·트레이너=`primary`) 전면적용. 공용 프리미티브 확립 — 아래 "핵심 파일 지도" 참조. **인라인 카드/폼/모달을 새로 손수 만들지 말 것 → 공용 컴포넌트 사용.**
- **🆕 위젯 톤은 "역할 이름" 체계**(`ea3ef91`) — 색 이름(amber/zinc/…)이 아니라 역할명으로 배정. 변형 추가 시 정적 맵에 역할명 리터럴로.
- **🆕 오운완(오늘 운동 완료) 집계는 전부 서버 RPC**(`19dae1d`) — 누적·연속·랭킹은 클라에서 계산하지 말 것(드리프트·max-rows 방지). 회원 read는 회원 RLS에 막히므로 `SECURITY DEFINER` 헬퍼 경유(`ce3e81b`).
- **🆕 무필터 전체조회는 `lib/fetchAllRows.js` 경유**(`ffc6370`, P0-6) — PostgREST Max rows(1000) 조용한 잘림 원천차단. `{data,error}` 드롭인. id(PK) 정렬 강제.
- ⚠️ **PWA iOS 스플래시는 철회됨**(`c20b947`) — iOS에서 끝내 미적용이라 이미지 17장·생성기·startupImage 전부 제거. 단 다크모드 검은화면 교정(`6eb68ce`, `--background`→`--color-bg`)은 **생존**(되돌리지 말 것).

## 이번 구간 완료 (묶음별 · 해시는 git log 대조)

**묶음 1 — 브랜드/워드마크 · PWA(07-19 저녁)**
- `c534e1f`→`de1ee0f`→`8e8ffde` PWA iOS 스플래시 17종 및 수정 → **`c20b947` 전량 REVERT**(iOS 미적용). 순효과 = 스플래시 없음.
- `6eb68ce` fix(theme): body가 Next 스타터 잔재(`--background`) 참조 → 다크모드 검은화면. 앱 토큰(`--color-bg`)으로 교정. **(생존 · 유지)**
- `ac3f00e` feat(brand): 워드마크 2색 공용 컴포넌트 + 로그인 슬로건 "ONLY FOR TRAINER".

**묶음 2 — 폰/모바일 레이아웃 위생(07-19 저녁)**
- `d37e56e` 로그아웃 플로팅 제거 → 설정 '내 정보'로 이관(전 화면 가림 해소).
- `3968be2` 1차OT 현장메모바 고정 해제 → 인플로우 카드(하단탭바 `bottom-0` 충돌 해소).
- `00f4cdd`·`e289715` 회원 등록/수정·가격·MBTI 폼 폰에서 1열 정리.
- `75d7d8e` 모달 카드 `max-h-[90vh] overflow-y-auto`(화면 밖 클립 방지).
- `8a2495e` `.tab-anim` fill-mode `both`→`backwards`(잔류 transform이 fixed 모달 가두던 문제).
- `15717f1`·`e31d6b3` 설정·OT 탭 섹션 제목을 카드 안으로 통일 + 아이콘 색 누락 교정.
- `0cf572c` 회원 모달 중앙 배치 + iOS 입력 포커스 자동확대 차단.
- `5d8db32` placeholder만 14px(긴 예시 문구 잘림 해소).

**묶음 3 — 오운완 기능군(07-20) · 스펙 있음**
- `3de2fd0` migration: `member_workout_log` 노쇼 누출 차단(`source<>'noshow'`).
- `19dae1d` migration: 오운완 집계 RPC 4개 + `trainer_reward` 테이블(서버 집계로 누적·드리프트·max-rows 해결, DEFINER 경계 revoke).
- `ecff4a1` 회원앱 오운완 카드(RPC 집계·뱃지·트레이너 포상 진행률).
- `29ba5df` 트레이너 오운완 랭킹(rpc 1콜 서버집계 · 내 실적 탭 · 횟수/연속일 토글).
- `d4ea78d` 포상 정의 서브탭(`trainer_reward` CRUD · 설정→포상 · 활성 토글·삭제 확인).
- `ce3e81b` migration fix: 회원 포상 read가 회원 RLS에 막혀 영구 false → `SECURITY DEFINER` 헬퍼로 직접 비교.
- `1d84a72`(07-21) 랭킹 설명 한 줄로(넘침 여백 제거).

**묶음 4 — 디자인 시스템 2단계 전면개편(07-21 새벽) · 산출물 문서만 있음, 구현 스펙 없음**
- `16dccf6` docs: PRD·화면정의서 신설 + 브랜드 가이드라인 v1.2.
- `5332691` DS 참조본 미러(`ONLY FOR TRAINER/`).
- `e3a0f5f` **1단계**: DS 토큰·Pretendard·유일침 로고 전면 적용.
- `c67782d` 2단계①: **AIBriefBlock 추출** + FirstOTAssist 적용.
- `9192d55` 2단계②: 역할색 토큰화 + 한글 라벨 자간 + Badge/FilterChip.
- `b4d4466` 한글 라벨 자간 스윕(Eyebrow 밖 69곳) + 워드마크 정정.
- `ce3a40c`·`da24516` 2단계③: PtReRegTab·SecondOTTab에 AIBriefBlock 적용.
- `e098dc7` 2단계④: **폼 필드 4종(`Field.jsx`)** + PasswordChange 첫 적용.
- `df1f868` 2단계⑤: **`Card` 신설 + 기존 Card→`ToneCard` 개명**.
- `f7ee1d5` 카드 셸 스윕: 인라인 37곳 → `Card`(18개 파일).
- `26a0332` 폼 스윕: `inputCls` 복사본 16개 → 공유 + NumberInput을 `Field`에 위임.
- `79ec0c7` 2단계⑥: **`Modal`을 DS 규격으로 + 포털 이전**.
- `f85ba7f` 손수 모달 4곳 → 공용 `Modal`(+size 축).
- `ea3ef91` 위젯 톤 색 이름 → 역할 이름 + 배정 교정.
- `5958d68` **MemberCard** → `Card` + 상태색 역할 토큰.
- `9e8ed6a` 2단계⑦: Button 치수 정렬(2단계 마지막 컴포넌트).

**묶음 5 — 베타 전 위생 + 07-21 폰UI/헤더/카피 마감**
- `d6ccad3` 베타 전 위생 2건(QC 데모 고지 + 인증 고착 방어).
- `29f0a13`·`954f3b7`·`8236bf3` 폰 폼 2열 깨짐·입력칸 안보임(ring→border, bg-card→bg-elevate 되돌림)·헤더 겹침/날짜칸 교정.
- `506f02f`·`6c3a815` 헤더 회원 드롭다운 제거 + 락업 위계(워드마크 앞·이름 보조).
- `bf3c276` 등록 폼 안내에서 내부 문서 참조(§1.5) 제거.
- `a019a2e`·`21cb2ab` 1차 피드백 ② 자유 메모 입력칸 제거(④ 종합소견과 중복) + ④ 설명의 ② 메모 언급 제거 + 세일즈 강도 설명을 트레이너 말로.
- `ac5902b`·`187a4b9`·`e9f322d` admin·트레이너 헤더 마크 정리(브랜드 심볼·indigo→fuchsia·PNG→BrandMark 벡터).

**묶음 6 — 성능·가드(07-21 낮) · 스펙 없음(직전 핸드오프 "다음 큐"에서 착수)**
- `1629670` perf+guard 종합점검 **P1 3건**: P1-9 재조회 루프(`RegisterDueToday` `[members]` deps → `memberKey` 패턴) · P1-10 `MyStats` useMemo 누락 · 프롬프트 입력 상한.
- `ffc6370` **P0-6**: 1000행 잘림 대비 — `lib/fetchAllRows.js` 신설, `admin/page.jsx`·`MyStats.jsx`의 무필터 4쿼리(`session_log`·`daily_workout_log`)에 적용. 계산식 불변(잘려있던 입력만 완전화 → 숫자 더 정확). ORDER BY 없던 비결정 잘림도 id 정렬로 해소. **(현 HEAD)**

## 다음 작업 큐 (우선순위)
1. **📌 문서 부채 정리(즉시):** 이 구간이 스펙/CLAUDE.md 없이 진행돼 문서가 코드보다 뒤처짐.
   - **(b) 사후 구현 스펙:** P1-9/P1-10(`1629670`)·P0-6(`ffc6370`)는 스펙 문서 부재 → 사후 기록. 디자인 2단계는 산출물 문서(PRD/화면정의서/브랜드가이드)만 있고 "무엇을 어떻게 구현" 스펙 부재.
   - **(c) CLAUDE.md 갱신:** 07-19(P2-21) 이후 미갱신 → 신규 파일(`fetchAllRows.js`·`Field.jsx`·`AIBriefBlock.jsx`·`Card`/`ToneCard`·`Modal`·`BrandMark`)·`trainer_reward`·Pretendard/DS 토큰·위젯톤 역할명 전환 미반영. §UI 규약·핵심 파일 지도 정정 필요.
2. **자동스케줄링:** 설계(`v2-설계-자동스케줄링-아키텍처.md`)만 있고 미구현 → 구현 스펙화.
3. **§8 정리(기존 잔여):** `lib/date.js` 통합 잔여(`kstYm`·`ymd`·`daysSince`·`fmtDay`·`todayStr`·`fmtDate`) · 중복 `fmtDay` 4벌 · `PtInbodyTab` `Sparkline` 자기복사본(공용 있음) · `public/` CRA 잔재 svg 제거.
4. **P0-6 후속:** 페이지 페처는 원천차단이지만, `daily_workout_log` 700행 도달 시 성능 관점에서 **서버 집계 RPC/뷰**로 이행 검토(현재는 <1000이라 결과 정확).
5. **백로그:** 음성일지 오프라인 큐(IndexedDB) · `member-auth` 인메모리 스로틀 Redis(베타 후) · P2 잔여(P2-18~20).

## push 후 검증 목록 (폰/배포본 · 이번 구간)
- **오운완:** 회원앱 오운완 카드(누적·연속·뱃지) 표시 · 트레이너 랭킹 탭(횟수/연속일 토글) · 설정→포상 정의 CRUD · 노쇼는 집계 제외.
- **디자인:** Pretendard 적용·유일침 로고/워드마크 2색 · AIBriefBlock(FirstOTAssist·PtReRegTab·SecondOTTab 3곳) 렌더 · 모달(포털) 열림·스크롤 정상 · 폼 필드 4종 정상.
- **폰 위생:** 등록/수정 폼 1열 · 헤더 겹침·드롭다운 제거 · iOS 확대 차단 · 다크모드 검은화면 없음.
- **P1:** RegisterDueToday 불필요 재조회 안 함 · MyStats 모달 여닫아도 무거운 재계산 없음.
- **P0-6:** 내 실적 급여·관리자 매출 숫자가 적용 전과 동일(현재 <1000이라 불변 = 드롭인 충실) · 에러 0.

## 결정 완료 (재론 X)
- **DS 표준화:** 카드=`Card`(+톤형 `ToneCard`), 폼=`Field`, 모달=공용 `Modal`(포털), AI 브리핑=`AIBriefBlock`, 로고=`BrandMark`. 인라인 재제작 금지.
- **위젯 톤:** 색 이름 아닌 **역할 이름**으로 배정(정적 맵 리터럴).
- **오운완 집계:** 서버 RPC only. 회원 read는 `SECURITY DEFINER` 헬퍼 경유.
- **PWA 스플래시:** iOS 미적용으로 **철회 확정**. 다크모드 `--color-bg` 교정은 유지.
- **P0-6:** 클라 무필터 전체조회는 `fetchAllRows` 경유(id 정렬). 날짜창으로 자르지 말 것(잔여 정확도).

## 핵심 파일 지도 (이번 구간 신설 ★)
- ★ `components/ui/AIBriefBlock.jsx` — AI 브리핑 공용 블록(FirstOTAssist·PtReRegTab·SecondOTTab 공유).
- ★ `components/ui/Field.jsx` — 폼 필드 4종(NumberInput 위임 포함).
- ★ `components/ui/Card.jsx` — 신규 `Card`(셸). 구 Card는 `ToneCard`로 개명(톤형).
- ★ `components/ui/Modal.jsx` — DS 규격 + 포털. (손수 모달은 이걸로 대체)
- ★ `components/ui/BrandMark`(벡터) + 워드마크 2색 컴포넌트.
- ★ `lib/fetchAllRows.js` — 페이지 페처(`{data,error}` 드롭인 · id 정렬 강제).
- ★ `trainer_reward` 테이블 + 오운완 집계 RPC 4개 — `docs/migrations/2026-07-20-ounwan-reward.sql`.
- `docs/migrations/2026-07-20-member-log-view-fix.sql` · 노쇼 누출 차단 기록본.
- `components/views/RegisterDueToday.jsx`·`MyStats.jsx` — P1-9/10 적용됨(재조회·useMemo).
- `app/admin/page.jsx`·`MyStats.jsx` — P0-6 `fetchAllRows` 적용(무필터 4쿼리).
- ⚠️ `CLAUDE.md` — **이번 구간 미반영(낡음)**. 큐 1-(c)로 갱신 대상.

## 이번 구간 문서 (docs/ · 근거)
- **있음:** `v2-스펙-오운완-출석뱧지랭킹-구현.md` · `v2-설계-자동스케줄링-아키텍처.md` · `PRD.md` · `화면정의서.md` · `브랜드가이드라인.html` · `ONLY FOR TRAINER/`(DS 미러) · migrations `2026-07-20-*.sql`.
- **없음(부채 · 큐 1):** 디자인 2단계 구현 스펙 · P1-9/P1-10 스펙 · P0-6 스펙 · 이 구간 핸드오프(=본 문서로 해소).
