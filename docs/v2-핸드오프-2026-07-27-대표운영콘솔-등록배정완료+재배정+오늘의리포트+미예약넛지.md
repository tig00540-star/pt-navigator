# 인계서 — 오직 트레이너(pt-navigator) · 등록·배정·재배정 + 오늘의 리포트 + 미예약 넛지 (2026-07-27)

> **범위:** 지난 세션 이어 ①회원 등록·배정·재배정(운영 콘솔) 마무리 확인 ②**원장 코칭용 "오늘의 리포트"**(트레이너 탭 하단·읽기전용) ③**미예약 넛지 "다음 예약 미정"**(오늘 할일 위젯).
> **현재 HEAD:** `0fa0781`. 이번 세션 코드 전부 커밋됨(아래). ⚠️ **push 여부는 대시보드/`git push origin main`로 직접 확인**(6커밋 미푸시일 수 있음).
> ⚠️ **먼저:** working tree에 `/lp` AuthGate WIP + untracked docs 다수 → 커밋 시 **`git add -A`/`git commit -am` 금지 · 스코프 add만.**

## 협업 방식 (그대로)
- **웹Claude:** 수정 스펙 작성 + 코드 리뷰(코드 내용 매칭·라인번호 X) + 순수함수 Node 검증. 앱 코드 직접 안 씀.
- **CC:** 로컬 구현. **대수:** `git diff` 붙여넣기 → 웹 리뷰 → 커밋은 대수.
- **제약:** additive 우선 · 로직/RLS/payload 불변 기본(변경 시 명시) · 새 파일 `git add` 먼저 · 배치별 1커밋 · Windows PowerShell(`npm.cmd run build/lint`) · SQL은 Supabase 수동+`docs/migrations/` 기록본.
- ⚠️ **mount-cache:** `lib/memberStatus.js`·`app/admin/page.jsx`(+ `CLAUDE.md`·`app/page.jsx` 사례 있음)는 재스테이징이 옛 바이트 → 이 파일들 리뷰는 대수가 `git diff`/`git show`로.

## 이번 세션 완료 (전부 리뷰 통과·커밋)

### A. 회원 등록·배정·재배정 (지난 세션 착수분 마무리 — 세션 시작 시 이미 커밋·푸시 확인)
- `47650b5` 회원 재배정(트레이너 인계·PT 전용·잔여 이월) · `0aad6ee` `handed_over` 플래그+잔여 가드 · `cc49949` 운영 탭 등록·배정 · `672c65b` MemberForm 추출+배정 seam.
- SQL 기록본 `docs/migrations/2026-07-27-session-log-handed-over.sql`(additive 1컬럼 · RLS 무변). ⚠️ **Supabase에 ALTER 실제 실행 여부만 한 번 실측 확인**(재배정 1건 테스트 → 원계약 잔여 0 빠지는지).

### B. 오늘의 리포트 — 원장 코칭용(읽기 전용) · 트레이너 탭(perf) 하단 섹션
제거된 "QC 모니터링"(하드코딩 데모)의 **실측 대체재**. 원장이 각 트레이너 OT·재등록 업무의 **부족한 부분**을 실제 내용까지 보고 **오프라인 대면 코칭**. 스펙 `docs/v2-스펙-트레이너OT재등록품질리포트-원장코칭-구현.md`.
- `6511800` 파생(관찰 충실도·브리핑 gaps·오늘 건별) · `b40d7f2` 컴포넌트 `TrainerQualityReport`+admin 배선 · `2032a27` **hidden 「오늘」뷰 누수 차단 가드 + 회원명 표기(`personName`)**.
- **뷰 2개(토글):** ①**오늘**(기본·`todayCases` — 오늘 진행 건별 코칭 카드) ②**전체 누적**(트레이너별 요약+드릴다운).
- **부족 4신호:** ①관찰 충실도 ②AI 브리핑 `data_gaps` ③일지 작성 ④클로징/재등록 결과·사유.
- **성공 건 규칙:** 실패·보류만 코칭 리스트 · 성공은 상단 카운트만(단 관찰 얇음/일지 미작성이면 그 신호는 잡힘).
- **당일 기준(A안):** 오늘 OT 관찰(created_at)·2차 OT(created_at)·수업(session_at). **마이그레이션 0.**
- ⚠️ **데이터 한계(화면 각주):** 재등록 결과는 '완료 시각' 컬럼 없어 오늘 판정 불가 → 전체 뷰만. 1차 사전무장 미캐시 → `data_gaps`는 2차만(1차는 관찰 충실도가 대신).
- **위치:** `app/admin/page.jsx`의 `{atab === "perf"}` 블록 **맨 끝**. **ATABS 8 그대로**(새 탭 없음).

### C. 미예약 넛지 — "다음 예약 미정"(오늘 할일 위젯)
잔여 있는 활성 PT인데 **앞으로 잡힌 booked 예약이 없는** 회원 → 다음 수업 예약 넛지(리텐션). 스펙 `docs/v2-스펙-미예약넛지-오늘할일-구현.md`.
- `cc29bda` 파생 `unbookedActiveMembers` · `376cccf` 위젯 `NoNextBookingToday` · `0fa0781` fetch/파생 분리(ChurnRisk 패턴 · refetch 폭주 방지).
- 위치: `TodoTab`의 `PastDueAppointments` 바로 뒤. 카드 탭 → `onSelect(id, 9)` → '오늘' 탭 스케줄 보드 스크롤(거기서 예약). **`app/page.jsx` 무변.**
- upcoming = booked & start_at>now만(과거 booked·canceled는 넛지 유지) · 잔여0·OT 제외 · gap 내림차순. handed_over 인계 계약은 `activeContract`가 자동 제외.

## 신규 순수함수 4종 (`lib/memberStatus.js` · 전부 웹Claude Node 검증)
- `observationQualityByTrainer(otRows, memberTrainer)` → Map: 트레이너별 1차 관찰 충실도(avgScore·thinList).
- `briefGapsByTrainer(otRows, contracts, memberTrainer)` → Map: OT 2차+재등록 브리핑 `data_gaps` 노출.
- `todayCases(otRows, logs, memberTrainer, {today})` → 오늘 건별 코칭 카드(성공 카운트만·hidden `mt.has` 가드).
- `unbookedActiveMembers(members, contracts, logs, appts, {nowISO})` → 미예약 넛지 대상.
- (로컬 헬퍼) `obsFilled`·`kstDay` — 기존 동명 함수 없음 확인.
- **공통:** admin/트레이너 앱이 이미 로드한 배열을 props로 받아 **파생만** · **마이그레이션 0 · RLS 무변 · write 0 · 신규 fetch 0**(미예약 넛지 위젯만 기존 테이블 self-fetch). hidden 필터 = 소비 컴포넌트 책임.

## 커밋 마무리 체크리스트 (대수)
1. **push:** `git push origin main` (이번 세션 6커밋 `47650b5`→`0fa0781` 푸시 확인 · 폰은 Vercel 배포본 하드리프레시).
2. **lint/build:** `npm.cmd run lint` · `npm.cmd run build` green 확인(특히 `NoNextBookingToday`의 `new Date()` in useMemo — ChurnRisk 관용과 동일이라 통과 예상).
3. **오늘의 리포트 SQL 실측:** handed_over ALTER 실행 여부 + 재배정 1건 스모크(위 A).
4. **(선택) 스펙/문서 커밋:** untracked 스펙 2개(`v2-스펙-트레이너OT재등록품질리포트-원장코칭-구현.md`·`v2-스펙-미예약넛지-오늘할일-구현.md`) 등 docs/ 정리 커밋 — **스코프 add만.**

## CLAUDE.md 동기화 포인트 (다음에 문서 반영 시)
- **admin 트레이너 탭(perf)**: 하단에 **'오늘의 리포트' 섹션**(`TrainerQualityReport` · 오늘/전체 토글 · 읽기전용 원장 코칭 · 제거된 QC의 실측 대체재) 추가. ATABS는 여전히 8.
- **트레이너 '오늘 할일'(TodoTab)**: **'다음 예약 미정' 위젯**(`NoNextBookingToday`) 추가 — 파생·새 저장 0 위젯 목록에 편입.
- **`lib/memberStatus.js` 신규 파생 4종**(위) 등재.
- **데이터 한계 메모:** 클로징/재등록엔 '결과 완료 시각' 컬럼 없음(오늘의 리포트 재등록 오늘판정 불가 근거) · 1차 사전무장 미캐시(브리핑 gaps 2차만).

## working tree 상태 (⚠️ 다음 세션 필독)
HEAD 커밋은 clean(코드 전부 커밋). 미커밋:
- **`components/AuthGate.jsx` (M)** — `/lp` 공개 랜딩 게이트 우회. **대수 진행 중 '/lp 랜딩+구독결제' 피처 WIP**. 단독 커밋 금지 — `/lp` 페이지·`marketing/`과 묶어.
- **modified:** `docs/MASTERPLAN.md`·`docs/PRD.md`·`docs/v2-스펙-세일즈북PPT-2차OT.md`. **deleted:** docs 2개.
- **untracked:** 이번+이전 세션 스펙/핸드오프 docs 다수 · `marketing/` · `supabase/`.
- → **커밋 시 반드시 스코프 add.**

## 미결·보류 (이월)
- **저위험 3종(지난 인계서):** ①dynamic import(`SecondOTTab`·`VoiceLogTab` 지연로드 · 제일 값짐) ②자가입력 저장 토스트(회원앱) ③memberStatus 리팩터(daysSince·버킷 중복 · mount-cache라 git diff 리뷰 · 마지막).
- **보류:** #2 KST(전원 KST 기기면 실피해 0 · 비-KST 환경 생기면 착수) · #4 4자리 레이트리밋(인프라) · 심층방어.
- **/lp 랜딩 + 구독결제**(별도 피처·WIP · PRD `docs/PRD-홈페이지-랜딩+구독결제.md`).
- **남은 기능 후보(3종 중):** 관찰 입력 음성화(voice-log 재사용 · 오늘의 리포트 '관찰 충실도'와 시너지) — OT/재등록 리포트·미예약 넛지는 이번 세션에 완료.

---
**다음 세션 시작점:** 이 파일 맨 위 붙이고 → `git --no-pager log --oneline -8`로 HEAD `0fa0781`(또는 이후) 확인 → 위 커밋 마무리 체크리스트(push·lint·SQL 실측) → 다음 기능은 **관찰 입력 음성화** 또는 저위험 3종(dynamic import) 또는 `/lp` 랜딩.
