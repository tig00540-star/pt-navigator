# 인계서 — 오직 트레이너(pt-navigator) · 2026-07-19
> 새 채팅 맨 위에 이 파일을 붙여넣고 이어간다.
> 직전 HEAD: 안정화 배치1-B까지 커밋·빌드/lint 초록·배포 단계.

## 협업 방식 (그대로 유지)
- **웹Claude(너):** 수정 스펙 작성 + 코드 리뷰
- **클로드코드:** 실제 코딩(내 로컬 Next.js 레포)
- **나(대수):** git diff 붙여넣기 → 너가 리뷰 → **커밋은 내가 직접**
- **제약:** 로직·RLS·payload 불변이 기본(의도적 변경은 반드시 명시). 표시/UI·additive 우선. 스펙은 **라인번호 말고 코드 내용으로 매칭**. 새 파일 생성 시 `git add` 먼저 안내.
- 환경: 윈도우 PowerShell(구버전) — `&&` 안 됨, `npm.cmd run build` 또는 `; if ($?)` 사용. `git commit -am`(새 파일은 add 먼저).

## 스택·규칙 요약
- Next.js 16.2.9(Turbopack) · Tailwind v4(`@theme` 토큰: `primary #dc2626`·`primary-strong #b91c1c`·`primary-soft`·`bg`·`card`·`elevate`·`line`·`ink`·`sub`·`muted`) · Supabase(RLS + "교훈1 하드닝" = 클라 write는 `.select()` 확인·데모 가드 필수) · Vercel.
- AI: OpenAI STT `gpt-4o-mini-transcribe` + Claude `claude-sonnet-5`. 라우트에 `maxDuration=60`.
- 탭 색: **OT=amber · PT=sky · 설정=브랜드 레드**. 레시피 = `app/page.jsx`의 `GROUP_TAB`(활성=진한 텍스트+언더바, 비활성=연한 같은색).
- 마이그레이션: `docs/migrations/*.sql` = **증분 로그**(Supabase 수동 실행). 베이스 테이블 4개(`user_table`·`ot_log`·`daily_workout_log`·`center_machine`)는 UI로 생성돼 DDL 기록 없음.

## 이번 세션 완료 (전부 커밋·빌드 초록)
1. 음성일지 라벨 `홈트레이닝 및 주의사항` → `개인운동시 주의사항`(화면 + 카톡 본문 `buildText`)
2. 음성인식 종목명: voice-log 프롬프트에 "사용 머신 목록 표기로 `name` 복원 + 반복음절 보존" → "로우로우 머신" 뭉개짐 해결
3. 종목세트기록: `SetsEditor`에 센터머신 커스텀 드롭다운(`ExerciseNameInput`) + voice-log `method` 종목별 차별화 규칙
4. 설정탭 개편: 서브탭(내 정보/정산/장비·큐/도서관)을 **헤더로 이관**해 OT/PT와 위치·스타일 통일(`SettingsView` sub prop + `app/page.jsx` `SETTINGS_SUBTABS`/`GROUP_TAB.settings`)
5. **B 기능(머신 큐잉 자산화):**
   - Phase 1: `center_machine.cues text[]`(마이그레이션 `2026-07-18-center-machine-cues.sql`) + `CenterMachineSettings` 큐 편집 + AI 초안 `POST /api/machine-cues`(Sonnet, owner)
   - Phase 2: `voice-log`가 등록 큐를 `method` 우선 기준으로 주입(`VoiceLogTab`이 `machine_cues` 전송 · 큐 없으면 AI 폴백)
6. **안정화 배치1(날-1):** voice-log·machine-cues `maxDuration=60` · `app/error.jsx`+`app/global-error.jsx` · **음성일지 실패 시 가짜일지 방지**(`failReal`, 데모는 503/마이크미지원만+`⚠️ 데모 예시` 워터마크) · AuthGate·회원앱·admin 진입부 hang catch · AI 라우트 3개 `console.error` 로깅 + 원문 `e.message` 클라 노출 차단

## 근거 문서(docs/)
- `docs/v2-종합점검-리포트-2026-07-19.md` — 코드 감사 리포트(§10 결정 반영)
- `docs/v2-스펙-머신별-큐잉템플릿.md` — B 설계

## 다음 작업 큐 (베타 도중 스윕 · 급하지 않음, 리포트 §9)
1. **correctness(싸고 조용 · 우선):**
   - `kstToday` 두 버전 로직 다름 → `lib/date.js`로 통합(§8). `PtInbodyTab.jsx` vs `RefundMember.jsx`.
   - **`SetsEditor` 드롭다운 open-상태가 위치기반** → 행 삭제 시 엉뚱한 행 드롭다운 열림(§7). *이번 세션에 내가 만든 거라 내 몫.* 안정 key 또는 리스트 변경 시 닫기.
2. AI JSON 렌더 방어: `.map()` 6곳 `.filter(Boolean)` + ot-brief `data_gaps` 타입가드(§3-4). 선례 `AcuteBriefView.jsx:13-15`.
3. DB 저장 `try/finally` 20여 곳 스윕(§4) — 버튼 영구 비활성 방지. (AI 경로는 이미 잘 됨, DB 경로만 빠짐)
4. `ChurnRiskToday` 날짜창+`useMemo` · 관리자 대시보드 월범위(§5).
5. `DEMO_MODE=1` 옵트인 전환(`requireTrainer` 현재 fail-open, §3-3) · 로컬은 `.env.local`.
6. `appointment` + `session_log.reg_*` 마이그레이션 기록본 백필 + `docs/migrations/README`(§6-3, §10-4).
7. 나중: `motion` 제거·아이콘 압축·중복헬퍼 정리(§8) · 스로틀 Redis(§7) · **음성일지 오프라인 큐**(별도 스프린트, §6-4/§11-1).

## 결정 완료 (재론 X)
- 데모 폴백: 실패=에러, 데모는 503/마이크미지원만+워터마크 ✅구현됨
- `requireTrainer`: `DEMO_MODE=1` 옵트인 예정(**미구현**)
- 큐 저장: `center_machine` 공용 1벌(대표 관리), per-trainer는 백로그
- 오프라인 큐: 별도 스프린트 / 관리자 집계: 베타엔 날짜필터, RPC 후 / 스로틀 Redis: 베타 후

## 핵심 파일 지도
- `app/page.jsx` — 메인 셸(탭 `TABS`/`GROUP_TAB`, 헤더 서브탭, `confirmPtActive`)
- `components/tabs/VoiceLogTab.jsx` — 음성일지(녹음→STT→요약, `buildText` 카톡본문, `machine_cues` 전송)
- `app/api/voice-log/route.js` — STT+Sonnet 요약(등록 큐 주입, `SUMMARY_SYSTEM`)
- `app/api/machine-cues/route.js` — 큐 AI 초안
- `components/views/CenterMachineSettings.jsx` — 보유장비+큐 편집(설정 "장비/큐" 탭)
- `components/views/SettingsView.jsx` — 설정 내용 분기(`sub` prop)
- `components/views/SetsEditor.jsx` — 종목세트 그리드(`ExerciseNameInput`)
- `lib/workout.js` — `machinesToStructured`(canonical 우선)·`buildExerciseSeries`
- `lib/requireTrainer.js` — API 인증(현재 fail-open → DEMO_MODE 전환 예정)
- `components/AuthGate.jsx` — 인증/구독 게이트
