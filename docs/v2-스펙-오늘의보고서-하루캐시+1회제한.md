# 스펙 델타 — 오늘의 보고서 **하루 캐시 + 하루 1회 제한** (localStorage · 2026-07-28)

> **요청:** ①보고서 한번 띄우면 **하루종일** 볼 수 있게(새로고침·재접속 유지) ②AI 생성을 **하루 1회로 제한**.
> **판정:** 결정적 블록은 무료 재계산 → 저장·제한 대상은 **AI(총평·코칭)** + **그날 스냅샷**. **`components/admin/OwnerBriefing.jsx` 한 파일만** 델타. 라우트·`ownerReportData`·마이그레이션 **0**. 앞 v2 스펙(commit `c4ced6c`) 위에 얹음.
> **동작:** 생성 성공 → 오늘 KST 날짜로 스냅샷을 localStorage 저장 → 마운트 시 오늘자 캐시 있으면 자동 로드(버튼 없이 · 종일 유지) → 오늘 캐시 있으면 **"다시 생성" 잠금**(AI 1회/일). 실패는 캐시 안 함(재시도 허용).
> **협업 그대로:** 웹=스펙+diff리뷰 / CC=구현 / 대수=커밋. additive · 로직 불변 · 스코프 add만 · `npm.cmd`.

---

## 0. 캐시 정책 (요지)

| 결과 | localStorage 저장 | 잠금(다시 생성 차단) | 이유 |
|---|---|---|---|
| `ready`(AI 성공) | ✅ 스냅샷 | ✅ | AI 하루 1회 · 종일 열람 |
| `premium`(basic·AI 차단) | ✅ 스냅샷(ai=null) | ✅ | 결정적 보고서 종일 열람 · 403 반복 방지(AI 비용 0) |
| `failed`(키·API·네트워크) | ❌ | ❌ | 일시 오류 → 재시도 허용 |

- 저장 내용 = `{ ymd(KST), report(ownerReportData 스냅샷·id만·회원명 없음), ai, aiState, generatedAt }`. **회원명·PII는 저장 안 함**(id만 · 렌더 시 `memberName`으로 해소).
- **KST 날짜 바뀌면 캐시 무효** → 다음날 자동으로 "받기" 버튼 복귀(새 보고서).
- localStorage 불가(프라이빗·용량)면 **무해 폴백**: 저장·자동로드 없이 현행(세션 메모리)대로 동작.

---

## 1. `OwnerBriefing.jsx` 델타

### 1-1. import
- react import에 **`useEffect`** 추가: `import { useMemo, useState, useEffect } from "react";`

### 1-2. 캐시 헬퍼 + 상태 (컴포넌트 본문 · 기존 report/ai/aiState 근처)
```js
const LS_KEY = "owner-report-v1";
function loadCache() { try { const r = localStorage.getItem(LS_KEY); return r ? JSON.parse(r) : null; } catch { return null; } }
function saveCache(o) { try { localStorage.setItem(LS_KEY, JSON.stringify(o)); } catch { /* 프라이빗·용량 = 무시 */ } }

const [generatedAt, setGeneratedAt] = useState(""); // 생성 시각 ISO(표시용)
const [locked, setLocked] = useState(false);        // 오늘 캐시 존재 = AI 1회 소진(다시 생성 차단)
const todayYmd = useMemo(() => new Date(new Date(nowISO).getTime() + 9 * 3600000).toISOString().slice(0, 10), [nowISO]);

// 생성시각 라벨: "오전 9시 12분 생성"
function genLabel(iso) {
  if (!iso) return "";
  const k = new Date(new Date(iso).getTime() + 9 * 3600000);
  const h = k.getUTCHours(), m = k.getUTCMinutes(), h12 = h % 12 || 12;
  return `${h < 12 ? "오전" : "오후"} ${h12}시 ${String(m).padStart(2, "0")}분 생성`;
}
```

### 1-3. 마운트 자동 로드 (오늘자 캐시 → 보고서 즉시 복원)
```js
// 마운트 1회: 오늘(KST) 캐시가 있으면 보고서 복원(버튼 없이 종일 유지 · AI 호출 0). nowISO는 마운트 고정이라 stable.
useEffect(() => {
  const c = loadCache();
  if (c && c.ymd === todayYmd && c.report) {
    setReport(c.report);
    setAi(c.ai || null);
    setAiState(c.aiState === "premium" ? "premium" : "ready");
    setGeneratedAt(c.generatedAt || "");
    setLocked(true);
  }
}, []); // eslint-disable-line react-hooks/exhaustive-deps
```

### 1-4. `genReport` — 성공/premium 시 저장 + 잠금 (교체)
```js
async function genReport() {
  if (!supabase || locked) return;   // ★ 하루 1회: 잠기면 무시(오늘 캐시 존재 시)
  const d = ownerReportData({ members, otRows, contracts, logs, appts, goals, ym, nowISO });
  setReport(d);
  setAi(null); setAiErr(""); setAiState("loading");
  const gAt = new Date().toISOString();  // 표시용 실시각(순수함수 아님 · 컴포넌트 UI)
  try {
    const aiInput = {
      ym: d.ym, yesterday: d.yesterday, today: d.today, month: d.month, members: d.members, watch: d.watch,
      top3: d.top3.map((c) => ({ title: c.kind === "trainer" ? `${nameOf(c.trainer_id)} — 관리 필요` : c.title, detail: c.detail, amount: c.amount ?? null })),
      trainerCoaching: d.trainerCoaching.map((c) => ({ trainer: nameOf(c.trainerId), msg: c.msg })),
      pipeline: { newCount: d.pipeline.newCandidates.length, reCount: d.pipeline.reCandidates.length, grandTotal: d.pipeline.grandTotal },
    };
    const { data: { session } } = await supabase.auth.getSession();
    const token = session?.access_token;
    const res = await fetch("/api/owner-report", {
      method: "POST",
      headers: { "Content-Type": "application/json", ...(token ? { Authorization: `Bearer ${token}` } : {}) },
      body: JSON.stringify({ input: aiInput }),
    });
    const body = await res.json().catch(() => ({}));
    if (!res.ok) {
      if (res.status === 403 && body?.code === "premium_required") {
        setAiState("premium"); setGeneratedAt(gAt); setLocked(true);
        saveCache({ ymd: todayYmd, report: d, ai: null, aiState: "premium", generatedAt: gAt }); // 결정적 보고서 종일 유지
        return;
      }
      setAiErr(body?.error || "AI 요약 생성 실패"); setAiState("failed"); return;  // 실패=캐시 안 함·재시도 허용
    }
    const aiVal = { headline: body?.headline || "", coaching: Array.isArray(body?.coaching) ? body.coaching : [] };
    setAi(aiVal); setAiState("ready"); setGeneratedAt(gAt); setLocked(true);
    saveCache({ ymd: todayYmd, report: d, ai: aiVal, aiState: "ready", generatedAt: gAt });  // ★ AI 1회/일 소진
  } catch {
    setAiErr("네트워크 오류"); setAiState("failed");  // 캐시 안 함
  }
}
```
> 변경점: 상단 `if (!supabase || locked) return;` · `gAt`(실시각) · premium/ready 분기에서 `setGeneratedAt/setLocked(true)/saveCache`. 실패·catch는 캐시·잠금 안 함. 나머지 로직 v2 그대로.

### 1-5. 헤더 — "다시 생성" ↔ "생성시각·내일 갱신" (교체)
기존 헤더 우측(다시 생성 + 인쇄) 중 **다시 생성 버튼만** 조건부로:
```jsx
<div className="flex items-center gap-2 shrink-0">
  {locked ? (
    <span className="text-[11px] text-muted">{genLabel(generatedAt)} · 내일 새 보고서</span>
  ) : (
    <button type="button" onClick={genReport} className="text-[11px] font-semibold text-muted underline underline-offset-2">다시 생성</button>
  )}
  <button type="button" onClick={() => window.print()} className="inline-flex items-center gap-1 text-[11px] font-semibold text-muted"><Printer className="h-3.5 w-3.5" /> 인쇄</button>
</div>
```
> `locked`면 "오전 9시 12분 생성 · 내일 새 보고서"(재생성 없음=AI 1회). `failed`(잠금 안 됨)면 "다시 생성" 노출 + 코칭 자리 "다시 시도"도 유지(둘 다 genReport · 성공하면 잠김).

### 1-6. 최하단 캡션 — 스냅샷 시점 명시 (문구만 보강)
보고서 카드 맨 아래 캡션을 스냅샷 기준 명시로:
```jsx
<p className="text-[10px] leading-relaxed text-muted">
  {generatedAt ? `${genLabel(generatedAt).replace(" 생성","")} 기준 스냅샷 · ` : ""}숫자·회원·금액은 실측 파생, 총평·코칭만 AI예요. 금액은 추정입니다.
</p>
```

---

## 2. 불변·성격
- **단일 파일**(`OwnerBriefing.jsx`) 델타 · **additive**. 라우트·`ownerReportData`·`ownerDailyDigest`·`requireTrainer`·`admin/page.jsx` 무변. 룰 카드·빈상태·룰 캡션 무변.
- **견고성 유지:** `setReport(d)`는 여전히 fetch 전 → AI 실패해도 결정적 블록 노출. 캐시는 그 위 계층.
- **비용:** AI 호출 = **하루 최대 1회/기기**(localStorage 잠금). 결정적 재계산은 무료(마운트 로드 시 AI 0). basic은 premium 캐시로 403도 1회만.
- **저장 안전:** localStorage 실패 시 무해 폴백(현행 세션 동작). PII(회원명) 미저장(id만).

## 3. 검증 (대수)
1. `npm.cmd run lint`·`build` green(`useEffect` 사용 · 미사용 0).
2. **하루종일 열람:** 보고서 받기 → 새로고침(F5)·탭 나갔다 재진입 → **버튼 없이 같은 보고서 자동 표시**(네트워크 탭에 `/api/owner-report` 호출 0). 헤더 "오전 N시 생성 · 내일 새 보고서".
3. **하루 1회:** 생성 후 "다시 생성" 안 보임(잠금). 재접속·새로고침해도 AI 재호출 0.
4. **날짜 경계:** (선택·수동) localStorage `owner-report-v1`의 `ymd`를 어제로 바꾸고 새로고침 → "받기" 버튼 복귀(캐시 무효).
5. **실패 재시도:** (네트워크 끊고) 받기 → 결정적 블록 뜨고 코칭 "다시 시도" · 잠금 안 됨 → 복구 후 재시도 성공하면 잠김.
6. **basic(있으면):** 받기 → 결정적 보고서 + "프리미엄 전용" · 잠금(새로고침해도 403 재호출 0).

## 4. 커밋 (스코프 add만)
- `feat(admin): 오늘의 보고서 하루 캐시 + AI 1회/일 제한(localStorage)` — `components/admin/OwnerBriefing.jsx`

## 5. 후속(선택)
- **하드 제한(계정당·기기 무관):** 서버 강제가 필요하면 새 테이블(`owner_report` account_id·ymd·headline·coaching·generated_at + RLS)로 승급 — 라우트 GET(오늘자 반환)/POST(있으면 재사용) 로직. 지금은 기기 소프트 제한.
- **수동 갱신 허용:** "오늘 다시" 버튼(확인 후 1회 더) 옵션.
