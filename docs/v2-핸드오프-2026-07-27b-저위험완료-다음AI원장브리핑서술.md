# 인계서 — 오직 트레이너(pt-navigator) · 저위험 3종 완료 + 다음 "오늘 챙길 것 AI 서술" (2026-07-27b)

> **다음 작업(확정):** admin '브리핑' 탭 **"오늘 챙길 것"의 AI 서술 계층**. 현재 룰 기반(`OwnerBriefing`·`ownerBriefing` top3)은 **이미 배포됨** → 그 결과를 재료로 **서버 AI(ot-brief 패턴·Sonnet)가 자연 문단화**. 근거: `docs/v2-스펙-AI원장브리핑-구현.md` **§8**(개념만 · "원장 요청 시 별도 스펙" → **이번에 그 별도 스펙을 씀**).
> **현재 HEAD:** `74db84e` · **미푸시 0(전부 push·배포됨).** 이번 세션 커밋: dynamic import `9d1c48b` · admin UX 정리(C1~C7) `9ccff6d`~`02f668f`(7커밋) · 자가입력 토스트 `a72cb94` · memberStatus 리팩터 `74db84e`.
> ⚠️ **working tree:** `/lp` AuthGate WIP + untracked docs 다수 → **커밋은 스코프 add만**(`git add -A`/`commit -am` 금지).

## 협업 방식 (그대로)
- **웹Claude:** 수정 스펙 작성 + 코드 리뷰(코드 내용 매칭·라인번호 X) + 순수함수 Node 검증. 앱 코드 직접 안 씀.
- **CC:** 로컬 구현. **대수:** `git diff` 붙여넣기 → 웹 리뷰 → 커밋은 대수.
- **제약:** additive 우선 · 로직/RLS/payload 불변 기본(변경 시 명시) · 새 파일 `git add` 먼저 · 배치별 1커밋 · Windows PowerShell(`npm.cmd run build/lint`) · SQL은 Supabase 수동+`docs/migrations/` 기록본.
- ⚠️ **mount-cache:** `lib/memberStatus.js`·`app/admin/page.jsx`·`app/page.jsx`·`CLAUDE.md`는 재스테이징이 옛 바이트 → 이 파일들 리뷰는 대수가 `git diff`/`git show HEAD:<path>`로. (그 외 `components/**`·`app/m/**`는 정상 스테이징 리뷰 가능.)

## 이번 세션 완료 (전부 스펙 작성 + CC 구현 + 웹 diff 리뷰 통과)
1. **dynamic import 지연로드** — `SecondOTTab`·`ObservationTab`(app/page.jsx)·`VoiceLogTab`(PtWorkoutTab) → `next/dynamic(ssr:false)`. 커밋 `9d1c48b`(push 됨). 스펙 `docs/v2-스펙-dynamic-import-지연로드-저위험.md`.
2. **admin UX 정리 (C1~C7)** — 접기 제거(매출·전환·리텐션·스케줄) · 퍼널 워딩(OT 회원(전체)/1차 OT 진행/2차 OT 진행/PT 등록 + 중첩 각주) · **탭 이름: 전환→"OT회원 현황" · 리텐션→"PT회원 현황"**(id는 funnel/retention 그대로) · **스케줄에 이번 달 트레이너별 OT/PT 진행수업**(OT=`ot_log`·PT=`daily_workout_log` 완료 · 신규 순수함수 `otSessionsThisMonthByTrainer` Node검증) · **트레이너 탭 '실데이터 요약'(센터 클로징률+재등록률) → `CenterMonthSummary`(이번 달 진행수업·신규등록·트레이너/활성회원)로 교체**. 스펙 `docs/v2-스펙-admin-UX정리-접기제거+워딩+탭이름+스케줄OTPT.md`. 신규 파일 `components/admin/CenterMonthSummary.jsx`. 커밋 `9ccff6d`~`02f668f`(7커밋 · C4=`eb234ca` otSessionsThisMonthByTrainer · C6=`fd71431` CenterMonthSummary · C7=`02f668f` 배선).
3. **회원앱 자가입력 저장 토스트** — cardio/photo/schedule 저장·삭제 성공에 공유 `useToast`/`Toast` 이식(성공 알림만·로직 불변). 파일 `app/m/[token]/page.jsx`. 스펙 `docs/v2-스펙-회원앱-자가입력-저장토스트.md`. 커밋 `a72cb94`.
4. **memberStatus 리팩터** — 이탈/만료 3함수 공통 헬퍼 추출(`makeDaysSince`·`bucketByMember`·`lastRealSession`) · **동작 100% 보존**(웹 Node 등가 검증). mount-cache라 git diff 리뷰함. 스펙 `docs/v2-스펙-memberStatus-리팩터-daysSince+버킷.md`. 커밋 `74db84e`. (⚠️ replace_all이 범위 밖 `unbookedActiveMembers`의 동일 블록도 치환→**원복**, 스펙 3함수만 반영.)

→ **저위험 3종(dynamic import·자가입력 토스트·memberStatus 리팩터) 전부 완료.**

## 커밋 마무리 체크리스트 (대수 · 새 챗 전/직후)
1. ✅ **커밋·push 완료** — 위 1~4 전부 커밋·`git push origin main` 반영(HEAD `74db84e` · 미푸시 0). lint/build green 확인함.
2. **lint/build green** — `npm.cmd run lint` · `npm.cmd run build`(admin UX + 토스트 + 리팩터 반영본).
3. **⚠️ memberStatus 리팩터 회귀 대조(필수)** — 리텐션(PT회원 현황) 탭 만료임박·이탈위험 **명수·리스트·정렬** / 리더보드 이탈위험 % / 트레이너 앱 ChurnRiskToday 가 **리팩터 전과 동일**한지(하나라도 다르면 롤백).
4. **스케줄 OT/PT 손계산 1건 대조** — 특정 트레이너 이번 달 `ot_log` 수·완료 수업 수와 화면 일치.
5. **(이월·미해결) handed_over SQL 실측** — 지난 인계서 항목. `session_log.handed_over` 컬럼 ALTER 실제 실행 여부 + 재배정 1건 스모크(원계약 잔여 0). Supabase 수동 · 아직 미확인.
6. **(선택) docs 스코프 커밋** — 이번 세션 스펙 5개(`v2-스펙-dynamic-import…`·`admin-UX정리…`·`회원앱-자가입력-저장토스트`·`memberStatus-리팩터…` + 이 핸드오프) 정리.

## 다음 작업 상세 — "오늘 챙길 것" AI 서술 계층
- **현 상태:** `ownerBriefing`(lib/memberStatus.js · 룰 기반 top3 조립 · 임팩트 ₩순) → `OwnerBriefing`(components/admin) 카드 렌더 = **배포됨**. AI 호출 0(결정적·무료).
- **목표(§8):** 룰 top3 결과를 **그대로 프롬프트 입력**으로 서버 AI가 원장용 자연어 브리핑 문단 생성. 지금 룰 배치 안 버림(재료로 재사용).
- **웹Claude 스펙에 담을 것(예상):** ①서버 라우트(`app/api/owner-brief` 신설 · Node runtime · `ot-brief`/`voice-log` 패턴 미러 · **Sonnet** `claude-sonnet-5` · maxDuration=180 · 프롬프트 서버조립 · JSON/텍스트 방어파싱 · **키 미설정 시 룰 결과 그대로 폴백**) ②프롬프트 대전제(원장 코칭 톤·숫자 단정 금지·추정 명시·은어 없음 · 룰 top3의 title/detail/amount/kind를 근거로) ③클라(`OwnerBriefing`) 배선 — **토글/캐시**(비용·대기 · 룰 뷰 기본, "AI 요약" 버튼/토글로 생성 · 결과 캐시) ④RLS/구독 게이트(AI라 premium 게이트 대상인지 — `my_account_status().access`/`auth_account_plan()` 확인) ⑤빈상태·실패 폴백(룰 뷰로 강등, 락아웃 금지).
- **주의:** admin/page.jsx·memberStatus.js는 mount-cache(diff 리뷰). AI 라우트는 서버 키 server-only. 비용 나가는 기능이라 **토글/캐시 필수**(관제 탭 열 때마다 자동 호출 금지).
- 근거 문서: `docs/v2-스펙-AI원장브리핑-구현.md`(룰 기반 정본 + §8 개념) · `CLAUDE.md`의 "admin 분석 대시보드"·"OT 지원 AI 대전제"·AI 라우트 규율.

## working tree 상태 (⚠️ 필독)
- **미커밋:** `components/AuthGate.jsx`(/lp 공개 랜딩 게이트 WIP · 단독 커밋 금지 — /lp 페이지·`marketing/`과 묶어) · docs 다수(modified/untracked) · `marketing/`·`supabase/` untracked · 임시 `admin-page-head.txt`(리뷰용 · 삭제해도 됨).
- → **커밋 시 반드시 스코프 add.**

## 미결·보류 (이월)
- **/lp 랜딩 + 구독결제**(별도 아크·WIP · PRD `docs/PRD-홈페이지-랜딩+구독결제.md`).
- **관찰 입력 음성화**(voice-log 재사용 · 오늘의 리포트 관찰 충실도와 시너지 · 중간 난이도).
- handed_over SQL 실측(위 체크리스트 5).

---
**다음 세션 시작점:** 이 파일 맨 위 붙이고 → `git --no-pager log --oneline -12`로 HEAD·이번 세션 커밋/푸시 확인 → 커밋 마무리 체크리스트(특히 리팩터 회귀 대조·handed_over SQL) → 다음 작업 = **"오늘 챙길 것" AI 서술 계층 스펙**(`v2-스펙-AI원장브리핑-구현.md` §8 기반).
