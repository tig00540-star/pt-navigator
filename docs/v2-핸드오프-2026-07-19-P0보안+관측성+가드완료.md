# 인계서 — 오직 트레이너(pt-navigator) · 2026-07-19 (P0 보안 + P1 관측성/가드 + P2-21 + 묶음 D 완료 · 다음: P1-9+10 성능)
> 새 채팅 맨 위에 이 파일을 붙여넣고 이어간다.
> ⚠️ **먼저 확인:** `git log --oneline -12` + `git status`. 이번 세션 커밋들이 **push 됐는지** 보고, "ahead of origin/main"이면 `git push origin main` → Vercel Ready 후 폰 검증.
> (참고: 세션 시작 시 origin/main=`faa041c`(아이콘)였고, 직전 `3d0c7d0 docs: 큐5 인계서+스펙`은 미push 상태였음 — 그것부터 밀렸는지도 같이 확인.)

## 협업 방식 (그대로 유지)
- **웹Claude(너):** 수정 스펙 작성 + 코드 리뷰
- **클로드코드:** 실제 코딩(로컬 Next.js 레포)
- **나(대수):** git diff 붙여넣기 → 너가 리뷰 → **커밋은 내가 직접**
- **제약:** 로직·RLS·payload 불변이 기본(의도 변경은 명시). 표시/UI·additive 우선. 스펙은 **라인번호 말고 코드 내용으로 매칭**. 새 파일은 `git add` 먼저.
- 환경: 윈도우 PowerShell — `&&` 안 됨. 빌드 `npm.cmd run build`, 린트 `npm.cmd run lint`, npx는 `npx.cmd`. 커밋은 `git commit -m "..." -- <경로>`로 범위 한정(배치별 1커밋 = revert 단위).
- ⚠️ **마운트 캐시 주의:** 이번 세션에서 웹Claude가 스테이징한 `lib/requireTrainer.js`·`.git/logs/HEAD`가 **옛 스냅샷을 캐시**해 혼동이 있었음. 파일 최신본이 의심되면 diff/`git log`를 **대수가 직접** 붙여 확인.

## 스택·규칙 요약 (이번 세션 갱신 반영)
- Next.js 16.2.9(Turbopack) · Tailwind v4(**밝은 테마 + 레드** `@theme`: `primary #dc2626`·bg `#eef1f5`·card·elevate·line·ink·sub·muted·primary-strong·primary-soft) · Supabase(**풀 RLS·account 스코프** `auth_account_id()` + 구독게이트 `my_account_status`·`auth_account_plan` · 교훈1 하드닝) · Vercel **Hobby + fluid compute(300s)**.
- AI: OpenAI STT `gpt-4o-mini-transcribe` + Claude `claude-sonnet-5`(voice-log·ot-brief 1·2차·machine-cues). **AI 라우트 `maxDuration=180`**(이번 세션 60→180). service_role 3개는 선언 없음=300s.
- **보안 게이트(이번 세션 핵심):** AI 라우트는 `requireTrainer`에서 active + **구독 access**(`my_account_status`) 둘 다 확인 → 미결제 403. 키 부재는 `DEMO_MODE=1`일 때만 스킵, 아니면 503(fail-closed).
- hang 방어 규율(유지·이번 세션 확장): 저장 `try/finally(setX(false))`, 조회 `finally{if(!cancelled)setLoading(false)}`. **로더/저장 스윕 완료**(P1-8).
- ⚠️ **CLAUDE.md는 이제 코드와 동기화됨(P2-21)** — 특히 "RLS 풀락·anon-open 금지". 다음 세션은 그걸 신뢰할 것.

## 이번 세션 완료 (해시는 git log로 확인 · 메시지 기준)
1. **묶음 A** `6db731a` `fix(security): AI라우트 인증 하드닝 — 미결제 계정 구독게이트(my_account_status) + 키부재 fail-open→DEMO_MODE 옵트인·503`
   - `lib/requireTrainer.js`: active 뒤 `sb.rpc("my_account_status")` → `access!==true`면 403(UI AuthGate와 동일 출처). 키부재 `DEMO_MODE=1`만 스킵·아니면 503+로그.
   - ⚠️ **`access`는 `trainer.active` 미포함** → active 검사와 별개로 둘 다 필요(확인함). 신규 베타 가입자는 default `inactive`라 온보딩 시 `subscription_status='active'` 세팅 필요(현재 DB엔 inactive 계정 0건 확인 → 커밋 안전했음).
2. **묶음 B** `0c2e886` 음성 업로드 크기 가드 — 48kbps(`audioBitsPerSecond`) + 업로드 전 4MB 클라 차단 + 서버 413 백스톱(Vercel body 4.5MB 소실 방지). `VoiceLogTab.jsx`+`voice-log/route.js`.
3. **마이크 캡처 제약** `b3a7e81` `getUserMedia`에 `noiseSuppression`·모노(`channelCount:1`)·`autoGainControl` — 헬스장 소음 대응(인코딩 전 감쇄). (스펙 외 추가 · 별도 커밋)
4. **P1-5 로깅** `feat(observability): API 인증·거부 경로 서버 로깅` — `requireTrainer` 5분기(401 인증/세션·403 비활성/구독·429) + `create-trainer`/`member-auth`/`member-revoke`. **🔒 토큰·끝4·비번 미로깅**, uid/member_id만. 레벨: 거부=warn·실패=error. 태그 `[requireTrainer]` 등.
5. **P1-6 에러바운더리** `fix(error-boundary)` — `app/error.jsx`·`global-error.jsx`가 `useEffect(()=>console.error(error))` + 화면에 `오류 코드:{digest}`(서버로그 대조 열쇠).
6. **P1-7 fail-open 교정** `fix(fail-open): useAccount try/catch + admin 버튼 isCenter 게이트` — `useAccount` await try/catch(실패 시 fail-closed defaults), **admin 버튼 `!isSolo`→`isCenter`(양성 검사)** = 로딩중·에러·solo 전부 숨김+깜빡임 제거. `lib/useAccount.js`+`app/page.jsx`.
7. **P1-8 로딩/저장 가드 스윕** `fix(hang)` — 로더 7(`try/catch/finally`+`finally{if(!cancelled)setLoading(false)}`) + 저장 3(PayrollConfirm·AdminAnnouncements post/pin/remove·TodoManual toggle/remove). 큐3가 `save`만 잡고 놓친 **로더·보조뮤테이션** 잔여를 채움. 9파일.
8. **P2-21 CLAUDE.md 동기화** `docs(CLAUDE.md)` — 밝은레드테마·TABS10·CRMTab제거·서버AI빌더(`buildVoiceReport`만 생존) 정정 + **⚠️RLS 풀락·anon-open 금지**로 §34·54 정정(가장 위험했던 낡음: "anon using(true) 정책 추가하라" → 제거). "⑦ 전까지 anon 전면개방" 잔재 구절도 정정.
9. **묶음 D** `perf(ai-routes): maxDuration 60→180` — fluid compute 300s 내 여유(실측 45s의 4배) + 폭주 상한. 낡은 주석("기본 10~15s") 정정. AI 라우트 3개. service_role은 **skip**(이미 300s·fluid compute active-CPU 과금이라 캡 실익 marginal·고아 안전마진만 감소).

**진단 근거:** `docs/v2-종합점검-리포트-2차-2026-07-19.md`(§ 기준). 리포트 P0-4는 fluid compute로 해소, P0-5(Hobby 상업금지)는 유료오픈 시 Pro 전환(경영), **P0-6(max-rows 1000)은 시한폭탄**(현재 전 테이블 1000행 미만 → 숫자 정확 · `daily_workout_log` 700행이 착수 트리거).

## 다음 작업 큐 (우선순위)
1. **P1-9 + P1-10 (성능 묶음):**
   - **P1-9** `RegisterDueToday.jsx` — deps가 `[members]`(배열 참조)라 어떤 state 변경에나 재조회. 형제 `ReapproachToday.jsx:36,55`의 **`memberKey=(members||[]).map(m=>m.id).join(",")` + `[today, memberKey]`** 패턴 복제. 또 `:21` `session_log.select("*")`를 실제 쓰는 컬럼으로 축소(형제들처럼). *(웹Claude가 RegisterDueToday·ReapproachToday는 이미 읽음 · `MyStats.jsx`만 더 읽으면 스펙 즉시 가능)*
   - **P1-10** `MyStats.jsx` — `useMemo`/`useCallback` 0개, `reportOpen` 모달 여닫을 때마다 O(계약×로그) 재계산(`:81-117`, `:113` `ptContracts.reduce(...remainingSessions...)`), `:88`·`:90` 로그 전체순회 2회(합칠 수 있음). 참고: `admin/page.jsx:253-289`(useMemo 17개)·`ChurnRiskToday.jsx:47`.
2. **§8 정리:** `lib/date.js` 통합(`kstYm`·`ymd`·`daysSince`·`fmtDay`·`todayStr`·`fmtDate` — `kstToday`는 이미 통합됨) · 중복 `fmtDay`(4벌: `app/m/[token]/page.jsx`·`MemberCardioSummary`·`MemberPhotoSummary`·`MemberScheduleSummary`) · `PtInbodyTab`의 `Sparkline` 자기복사본(공용 `components/ui/Sparkline` 있음) · `public/`의 CRA 잔재 svg(`next.svg`·`vercel.svg`·`file.svg`·`globe.svg`·`window.svg`) 제거.
3. **P0-6 (시한폭탄 · 700행 트리거):** admin/MyStats/ChurnRisk/RegisterDue의 무필터 `select("*")`가 1000행에서 조용히 잘림. ⚠️ **날짜창으로 자르면 안 됨**(잔여는 계약 전체이력 필요 → 과대계산). **월별 매출 집계와 잔여 계산 분리** or 서버 집계 RPC/뷰. `ORDER BY` 없어 비결정 잘림도 동반.
4. **백로그:** 음성일지 오프라인 큐(IndexedDB blob 재시도) · `member-auth` 인메모리 스로틀 Redis(베타 후) · P2 잔여(P2-18 DB에러 원문 클라노출·P2-19 D3_FORCE·P2-20 라벨미스 등).

## push 후 검증 목록 (폰/배포본)
- **묶음 A:** 구독 `inactive` 계정 JWT로 `curl /api/ot-brief` → **403 "구독이 필요합니다"**. 본인(active)은 AI 정상.
- **묶음 B:** 1~3분 녹음 정상 생성 · (선택) 시끄러운 환경 숫자 정확도 · blob 4MB 초과 시 업로드 전 차단.
- **P1-5:** 위 403이 Vercel 함수 로그에 `[requireTrainer] 403 구독 미활성 status=inactive`로 찍히는지(토큰 미로깅 확인).
- **P1-7:** solo 계정 → 원장 대시보드 버튼 안 보임·깜빡임 없음. center → 로드 후 정상.
- **P1-8:** (오프라인) 설정 탭 조회 실패해도 "불러오는 중…" 고착 안 됨 · 저장 중 끊겨도 버튼 재활성.
- **묶음 D:** 긴 AI 생성이 60s에서 안 잘림(평소 생성은 동일).

## 결정 완료 (재론 X)
- **P0-2 게이트:** `DEMO_MODE=1` 명시 옵트인(NODE_ENV 아님 — Vercel은 Preview·Prod 둘 다 NODE_ENV=production). 키 있으면 분기 안 탐 = 정상배포 무영향.
- **P0-1:** UI 결제벽(`my_account_status.access`)과 **동일 출처**를 API도 사용(드리프트 0). active와 access는 별개 검사.
- **음성 크기:** 48kbps + 4MB 클라 가드(하드) + 서버 413 백스톱. 오프라인 큐는 별도 스프린트.
- **maxDuration:** AI 180(선언 제거=300s는 비권장). service_role은 300s 유지(캡 실익 marginal).
- **P0-6:** 클라 날짜창 금지(정확도) → 서버 집계 RPC로, 700행 도달 시.
- **로깅:** 토큰·끝4·비번 절대 미로깅(uid/member_id만). 거부=warn·실패=error.

## 핵심 파일 지도 (갱신)
- `lib/requireTrainer.js` — AI 라우트 게이트(active + `my_account_status.access` + DEMO_MODE + 5분기 로깅).
- `lib/useAccount.js` — try/catch + `isCenter`(양성 게이트). `app/page.jsx:698` admin 버튼은 `isCenter`.
- `components/tabs/VoiceLogTab.jsx` — 48kbps·4MB 가드·noiseSuppression·모노. `app/api/voice-log/route.js` — 413 백스톱·maxDuration 180.
- `app/error.jsx`·`app/global-error.jsx` — console.error(error)+digest.
- 로더/저장 가드 완료: `AdminAnnouncements`·`PayrollConfirm`·`TodoManual`·`TrainerGoalSetter`·`TrainerProfileSettings`·`TrainerLibrary`·`PtPricingSettings`·`CenterMachineSettings`·`ObservationTab`.
- `components/views/RegisterDueToday.jsx` — **다음 타깃(P1-9)**: `[members]` deps → `memberKey`. `ReapproachToday.jsx`가 정답 패턴.
- `components/views/MyStats.jsx` — **다음 타깃(P1-10)**: useMemo 0개.
- `docs/migrations/2026-07-16-b1*.sql`·`b2-*.sql` — 구독/게이트 DDL(access·plan). `CLAUDE.md` — 코드 동기화됨(신뢰 가능).

## 이번 세션 스펙 문서 (docs/ · 근거)
- `v2-스펙-묶음A-P0보안-requireTrainer.md` · `v2-스펙-묶음B-P0-3-음성크기가드.md` · `v2-스펙-P1관측성-로깅+에러바운더리.md` · `v2-스펙-P1-7-8-로딩상태가드.md` · `v2-스펙-P2-21-CLAUDE-md정정.md` · `v2-스펙-묶음D-maxDuration.md`
- 진단: `v2-종합점검-리포트-2차-2026-07-19.md` · 직전 인계서 `v2-핸드오프-2026-07-19-큐5+백필완료.md`
