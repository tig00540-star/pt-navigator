# 스펙 — 원장 분석 대시보드 #5 가동률·스케줄 (2026-07-25)

> **범위:** admin(원장) 분석 대시보드 **#5 가동률·스케줄** — 요일×시간 예약 **밀도 히트맵**(=빈 시간대) · 트레이너별 예약/완료/취소/미처리 · 노쇼율 · 미처리 예약 리스트. **새 '스케줄' 탭.**
> **협업 방식 그대로:** 웹Claude=스펙+diff리뷰+순수함수 실행검증 / CC=로컬 구현 / 대수=커밋. additive 우선 · 로직·RLS·payload 불변 · 코드 내용으로 매칭 · 새 파일 `git add` 먼저 · 배치별 1커밋 · Windows PowerShell(`npm.cmd`).
> **웹Claude 실행검증 선행 완료:** 신규 순수함수 3종 + 재사용 `pastDueAppointments`를 실제 `memberStatus.js`에 붙여 토이 데이터로 로컬 실행 — 집계 정확·런타임 0. KST 요일/시간 경계(UTC 일요일 16:00Z가 KST 월요일 01:00로 잡힘)·완료율·노쇼율(voided 제외)·미처리 booked·빈데이터 가드 전부 통과.

---

## 0. 결정·판정 요약 (대수 확정)

- **가동률 = 밀도 히트맵으로 대체(옵션 A 확정).** 진짜 가동률(예약÷가능슬롯 %)은 **트레이너 근무시간/정원 스키마가 없어 불가**(#3 미수금과 같은 경계). 대신 정원 없이 정직하게 나오는 것: 요일×시간 예약 **밀도**(빈 시간대 = 저밀도 셀) + 트레이너별 절대 수업량·완료율·노쇼율. 정원 기반 진짜 %가 필요하면 별도 스프린트(근무시간 입력 테이블·UI).
- **appointment 로드 = 최근 90일 윈도우(확정).** 스케줄 패턴은 최근이 의미 있고 가벼움. `.gte("start_at", 90일전)`. 화면에 "최근 90일" 명시. (매출·급여의 "날짜창 금지"와 다름 — 저긴 누적총액이라 잘리면 틀리고, 여긴 애초에 패턴 지표라 윈도우가 맞다.)
- **배치 = 새 '스케줄' 탭(확정).** ATABS: 트레이너·매출·전환·리텐션·**스케줄**·QC·급여·운영.

| 요소 | 방법 | additive? |
|------|------|-----------|
| 요일×시간 밀도 히트맵 | **`apptWeekdayHourGrid`(신규 순수함수)** | ✅ |
| 트레이너별 예약/완료/취소/미처리 | **`apptOutcomeByTrainer`(신규 순수함수)** | ✅ |
| 노쇼율 | **`noshowByTrainer`(신규 순수함수)** — `daily_workout_log.source='noshow'` 원천(기존 `logs`) | ✅ |
| 미처리 예약 리스트 | `pastDueAppointments`(기존 재사용) | ✅ 재사용 |
| appointment 로드 | admin **새 fetch 1개**(최근 90일 · owner RLS 이미 허용 · 마이그레이션 0) | ✅ 쿼리 1개 추가 |
| **진짜 가동률 %(정원 대비)** | — | ❌ 스키마 없음 → 밀도로 대체 |

**핵심 경계 2가지(화면에 반드시 명시):**
1. **"가동률"이 아니라 "예약 밀도"** — 정원 분모가 없어 %가 아님. 캡션으로 정직하게.
2. **노쇼는 예약(appointment)이 아니라 수업일지(`daily_workout_log.source='noshow'`) 기준** — appointment엔 noshow 상태가 없다(booked/done/canceled뿐). 별개 원천임을 캡션에.

---

## 1. 데이터 소스

| prop | 원천 | 상태 |
|------|------|------|
| **`appts`** | **`appointment` select(최근 90일 · canceled 포함) — 신규 fetch** | **추가** |
| `logs` | `daily_workout_log`(기존 로드) — 노쇼 원천 | 기존 |
| `members` | `rows`(user_table) — user_id→trainer_id 맵용 | 기존 |
| `trainers` | `trainers`(id,name) | 기존 |
| `ym` | KST 'YYYY-MM' — 노쇼 이달 집계(선택) | 기존 |

**appointment 스키마(2026-07-19 기록본):** `id·account_id·trainer_id·user_id·start_at(timestamptz)·status('booked'|'done'|'canceled')·log_id·created_at`. RLS `auth_read_appointment`가 **owner에게 계정 전체 SELECT 허용**(`auth_is_owner() or trainer_id=auth.uid()`). ⚠️ **admin fetch는 `canceled`를 포함**해야 취소율이 나온다(ScheduleBoard는 `.neq("status","canceled")`로 빼지만, 분석은 포함).

**⚠️ hidden:** 히트맵·트레이너표는 `appts.trainer_id`로 직접 집계 → 회원 hidden 무관. **노쇼만** `user_id→trainer` 맵이 필요한데, 여기선 **전체 `rows`로 맵을 만든다**(환불/숨김 회원의 과거 노쇼도 트레이너 이력이라 집계에 포함 — 회원 명단 지표가 아니라 트레이너 행동 집계라 #1/#4의 visible 규율과 다름).

---

## 2. `lib/memberStatus.js` — 신규 순수함수 3종 (파일 끝 append · 검증본)

> 전부 순수(기준시각 주입 · 모듈 내 `new Date()`(무인자)/`Date.now()`/`Math.random()` 금지 · `kstYm` 재사용). `pastDueAppointments`(기존 · line 근처 `closingDueSoon` 위)는 그대로 재사용.

```js
/* =========================================================================
   #5 가동률·스케줄 — 순수 파생 (admin fetch 배열 주입).
   가동률 진짜 %는 정원 데이터가 없어 불가 → '예약 밀도'로. 노쇼는 daily_workout_log 원천.
   ========================================================================= */

/**
 * 요일×시간 예약 밀도 그리드 — 최근 예약(canceled 제외)을 KST 요일(Mon=0..Sun=6)×시간 카운트.
 * 정원 분모가 없어 '가동률 %'가 아니라 '밀도'(몰림/빔). start_at(UTC ISO)→KST 벽시계로 요일·시 판정(순수).
 * @param {Array} appts  appointment 행
 * @param {{startHour?:number,endHour?:number}} [opts]  표시 시간범위(기본 6~24 · 밖은 제외)
 * @returns {{hours:number[], grid:number[][], max:number, total:number, byWeekday:number[], byHour:number[]}}
 */
export function apptWeekdayHourGrid(appts, { startHour = 6, endHour = 24 } = {}) {
  const hours = [];
  for (let h = startHour; h < endHour; h++) hours.push(h);
  const hIndex = new Map(hours.map((h, i) => [h, i]));
  const grid = Array.from({ length: 7 }, () => new Array(hours.length).fill(0)); // [Mon..Sun][hourIdx]
  let total = 0, max = 0;
  for (const a of Array.isArray(appts) ? appts : []) {
    if (!a || a.status === "canceled") continue;
    if (typeof a.start_at !== "string") continue;
    const t = Date.parse(a.start_at);
    if (Number.isNaN(t)) continue;
    const kst = new Date(t + 9 * 3600 * 1000);        // KST 벽시계(순수: iso로만 · now 안 읽음)
    const wd = (kst.getUTCDay() + 6) % 7;             // Mon=0..Sun=6
    const ci = hIndex.get(kst.getUTCHours());
    if (ci == null) continue;                          // 표시 범위 밖 시간 제외
    grid[wd][ci] += 1; total += 1;
    if (grid[wd][ci] > max) max = grid[wd][ci];
  }
  const byWeekday = grid.map((row) => row.reduce((s, n) => s + n, 0));
  const byHour = hours.map((_, i) => grid.reduce((s, row) => s + row[i], 0));
  return { hours, grid, max, total, byWeekday, byHour };
}

/**
 * 트레이너별 예약 결과 — 예약 총수·완료·취소·미처리(과거 booked). nowISO 주입(순수).
 * doneRate = done/(done+canceled) (해소된 예약 중 완료율 · 미처리는 아직 미해소라 분모 밖).
 * @returns {Array<{trainer_id,total,done,canceled,booked,pastDue,doneRate}>} 예약수 내림차순.
 */
export function apptOutcomeByTrainer(appts, nowISO) {
  const now = typeof nowISO === "string" ? nowISO : "";
  const map = new Map();
  const get = (tid) => { let c = map.get(tid); if (!c) { c = { total: 0, done: 0, canceled: 0, booked: 0, pastDue: 0 }; map.set(tid, c); } return c; };
  for (const a of Array.isArray(appts) ? appts : []) {
    if (!a) continue;
    const c = get(a.trainer_id ?? "unknown");
    c.total += 1;
    if (a.status === "done") c.done += 1;
    else if (a.status === "canceled") c.canceled += 1;
    else if (a.status === "booked") { c.booked += 1; if (typeof a.start_at === "string" && now && a.start_at < now) c.pastDue += 1; }
  }
  return [...map.entries()]
    .map(([trainer_id, v]) => ({ trainer_id, ...v, doneRate: (v.done + v.canceled) ? v.done / (v.done + v.canceled) : null }))
    .sort((a, b) => b.total - a.total);
}

/**
 * 트레이너별 노쇼율 — daily_workout_log 기준(★appointment 아님 · appointment엔 noshow 상태 없음).
 * done = voided 아니고 source!=='noshow' · noshow = source==='noshow'. ym 주면 이달만.
 * @param {Array} logs
 * @param {Map} memberTrainer  Map(user_id → trainer_id) — 전체 회원으로 구성(hidden 포함, 트레이너 이력)
 * @returns {Array<{trainer_id,done,noshow,total,noshowRate}>} 노쇼율 내림차순.
 */
export function noshowByTrainer(logs, memberTrainer, { ym } = {}) {
  const mt = memberTrainer instanceof Map ? memberTrainer : new Map();
  const map = new Map();
  const get = (tid) => { let c = map.get(tid); if (!c) { c = { done: 0, noshow: 0 }; map.set(tid, c); } return c; };
  for (const l of Array.isArray(logs) ? logs : []) {
    if (!l || l.voided) continue;
    if (ym && !(typeof l.session_at === "string" && kstYm(l.session_at) === ym)) continue;
    const tid = mt.get(l.user_id) ?? "unknown";
    const c = get(tid);
    if (l.source === "noshow") c.noshow += 1; else c.done += 1;
  }
  return [...map.entries()]
    .map(([trainer_id, v]) => ({ trainer_id, ...v, total: v.done + v.noshow, noshowRate: (v.done + v.noshow) ? v.noshow / (v.done + v.noshow) : null }))
    .sort((a, b) => (b.noshowRate ?? -1) - (a.noshowRate ?? -1));
}
```

**검증 로그(웹Claude 로컬 실행 · 토이 데이터):**
- `apptWeekdayHourGrid`: 비취소 5건 · 월요일 3건(UTC 일요일 16:00Z가 KST 월요일로 정확 분류) · 월9시 셀 2 → max 2 · 범위 밖(10~22 지정 시 9시·1시) 제외 · `byHour 합==total` ✅
- `apptOutcomeByTrainer`: T1 done2/canceled0/booked1/pastDue1 → doneRate 1 · T2 canceled1/pastDue1 → doneRate 0 ✅ (`start_at < nowISO` 문자열 UTC 비교로 미처리 판정)
- `noshowByTrainer`: T1 done2/noshow1 → 1/3 · voided 제외 ✅
- `pastDueAppointments`: 과거 booked만(취소·완료·미래 제외) start_at 오름차순 ✅
- 빈 데이터 → total 0 / [] 가드 ✅

---

## 3. `app/admin/page.jsx` 배선

### (a) import + state + ATABS 탭 추가
- import: `import ScheduleAnalytics from "@/components/admin/ScheduleAnalytics";`
- state: `const [appts, setAppts] = useState([]);`
- **ATABS에 '스케줄' 탭 추가** — '리텐션' **다음**(QC 앞):
```js
const ATABS = [
  { id: "perf",      label: "트레이너" },
  { id: "revenue",   label: "매출" },
  { id: "funnel",    label: "전환" },
  { id: "retention", label: "리텐션" },
  { id: "schedule",  label: "스케줄" },   // ← 추가(#5)
  { id: "qc",        label: "QC" },
  { id: "payroll",   label: "급여" },
  { id: "ops",       label: "운영" },
];
```

### (b) appointment fetch — **최근 90일 · 비차단**
`Promise.all`에 `ap` 추가(마지막). 윈도우 컷오프는 effect 안에서 계산(`Date.now()`는 effect라 OK):
```js
const apptCutoff = new Date(Date.now() - 90 * 24 * 3600 * 1000).toISOString(); // 최근 90일
const [u, o, c, l, tr, ps, pr, tg, ap] = await Promise.all([
  // …기존 8개(user_table·ot_log·session_log·daily_workout_log·trainer·pay_scheme·payroll_run·trainer_goal)…
  fetchAllRows(() => supabase.from("appointment").select("*").gte("start_at", apptCutoff)),
]);
```
그리고 `setGoals(tg.data || []);` 다음에:
```js
setAppts(ap.data || []);
```
> ⚠️ **`ap.error`를 `firstErr`에 넣지 말 것**(goals와 동일 비차단 — appointment 없거나 실패해도 페이지 안 막음, 스케줄 탭만 빈상태). **fetchAllRows로 90일 창 안을 끝까지 페이지네이션**(창은 좁지만 다트레이너면 1000행 넘을 수 있어 안전빵). ⚠️ **canceled 포함**(취소율 계산 · `.neq` 걸지 말 것).

### (c) 섹션 렌더 (리텐션 렌더 근처)
```jsx
{/* ===== 가동률·스케줄 (스케줄 탭 · #5) ===== */}
{atab === "schedule" && (
<section className="mb-8">
  <ScheduleAnalytics appts={appts} logs={logs} members={rows} trainers={trainers} ym={ym} />
</section>
)}
```

---

## 4. `components/admin/ScheduleAnalytics.jsx` (신규)

> 4블록: ① 요약 타일(예약수·완료율·노쇼율·미처리) · ② 요일×시간 밀도 히트맵 · ③ 트레이너별 스케줄 표 · ④ 미처리 예약 리스트. **파생함수 호출·렌더만**(RetentionConsole 패턴). **색: 좋음=cyan · 주의=rose · 밀도=primary(불투명도). emerald 금지.** 정직 캡션 2개 필수(가동률 아님·노쇼 원천).

### 4-1. 골격
```jsx
"use client";
/* =========================================================================
   #5 가동률·스케줄 — admin '스케줄' 탭.
   요일×시간 예약 밀도(=빈 시간대) · 트레이너별 예약/완료/취소/미처리 · 노쇼율 · 미처리 예약.
   admin이 로드한 appts(최근90일)+logs+members+trainers를 props로 받아 파생·렌더만.
   ⚠️ 밀도는 '가동률 %' 아님(정원 데이터 없음) · 노쇼는 수업일지(source='noshow') 기준(예약 아님).
   색: 좋음 cyan · 주의 rose · 밀도 primary(불투명도). emerald 금지.
   ========================================================================= */
import { useMemo, useState } from "react";
import { CalendarClock, AlertTriangle, UserX, LayoutGrid } from "lucide-react";
import { apptWeekdayHourGrid, apptOutcomeByTrainer, noshowByTrainer, pastDueAppointments } from "@/lib/memberStatus";
import { personName } from "@/lib/format";
import Card from "@/components/ui/Card";
import ToneCard from "@/components/ui/ToneCard";

const DAYS = ["월", "화", "수", "목", "금", "토", "일"];
const START_HOUR = 6, END_HOUR = 24;
const pctText = (r) => (r == null ? "—" : Math.round(r * 100) + "%");
const LIMIT = 20;

export default function ScheduleAnalytics({ appts = [], logs = [], members = [], trainers = [], ym }) {
  const [nowISO] = useState(() => new Date().toISOString()); // 마운트 1회 고정

  const grid = useMemo(() => apptWeekdayHourGrid(appts, { startHour: START_HOUR, endHour: END_HOUR }), [appts]);
  const outcome = useMemo(() => apptOutcomeByTrainer(appts, nowISO), [appts, nowISO]);
  const memberTrainer = useMemo(() => new Map(members.map((m) => [m.id, m.trainer_id])), [members]); // 전체(hidden 포함)
  const noshow = useMemo(() => noshowByTrainer(logs, memberTrainer, {}), [logs, memberTrainer]); // 전체기간(ym 생략)
  const pastDue = useMemo(() => pastDueAppointments(appts, nowISO), [appts, nowISO]);

  const nameOf = (tid) => personName(trainers.find((t) => t.id === tid)?.name) || (tid === "unknown" ? "미배정" : String(tid).slice(0, 8));
  const memberName = (uid) => personName(members.find((m) => m.id === uid)?.name) || String(uid).slice(0, 8);

  // 센터 롤업(컴포넌트 합산 — 단순 reduce)
  const totalDone = outcome.reduce((s, t) => s + t.done, 0);
  const totalCanceled = outcome.reduce((s, t) => s + t.canceled, 0);
  const centerDoneRate = (totalDone + totalCanceled) ? totalDone / (totalDone + totalCanceled) : null;
  const nsDone = noshow.reduce((s, t) => s + t.done, 0), nsNo = noshow.reduce((s, t) => s + t.noshow, 0);
  const centerNoshowRate = (nsDone + nsNo) ? nsNo / (nsDone + nsNo) : null;
  const pastDueCount = outcome.reduce((s, t) => s + t.pastDue, 0);

  // 가장 붐비는 슬롯(라벨용)
  const peak = useMemo(() => {
    let best = null;
    grid.grid.forEach((row, wd) => row.forEach((n, ci) => { if (n > 0 && (!best || n > best.n)) best = { wd, hour: grid.hours[ci], n }; }));
    return best;
  }, [grid]);

  // …4-2 ~ 4-5 렌더
}
```

### 4-2. 블록① 요약 타일 (4개)
- 역할 1줄: `<p className="text-[12px] leading-relaxed text-sub">예약이 <b className="text-ink">언제 몰리고</b>, 누가 <b className="text-ink">잘 채우는지</b> — 최근 90일.</p>`
- 타일(RetentionConsole `SummaryTile` 패턴 · accent cyan/rose/ink):
  - **최근 90일 예약** `{grid.total}건` (sub: 취소 제외 · 밀도 기준)
  - **완료율** `{pctText(centerDoneRate)}` accent cyan (sub: 완료 {totalDone} / 취소 {totalCanceled})
  - **노쇼율** `{pctText(centerNoshowRate)}` accent rose (sub: 수업일지 기준 · 노쇼 {nsNo}건)
  - **미처리 예약** `{pastDueCount}건` accent rose (sub: 시간 지났는데 완료·취소 안 함)

### 4-3. 블록② 요일×시간 밀도 히트맵
- 헤더 `LayoutGrid` + "요일·시간대 예약 밀도" + peak 라벨(`{DAYS[peak.wd]} {peak.hour}시가 가장 붐빔` 있으면).
- 그리드: 왼쪽 시간축(START~END-1), 상단 요일축(월~일). 각 셀 배경 = **primary 불투명도**(밀도). **동적 클래스 금지 → inline style로**(purge 무관):
```jsx
<div className="overflow-x-auto">
  <div className="min-w-[560px]">
    <div className="flex">
      <div className="w-10 shrink-0" />
      {DAYS.map((d, i) => <div key={i} className={`flex-1 pb-1 text-center text-[11px] font-semibold ${i===6?"text-rose-600":i===5?"text-sky-600":"text-sub"}`}>{d}</div>)}
    </div>
    {grid.hours.map((h, ci) => (
      <div key={h} className="flex items-stretch">
        <div className="w-10 shrink-0 py-1 pr-1 text-right font-mono text-[10px] text-muted">{h}시</div>
        {DAYS.map((_, wd) => {
          const n = grid.grid[wd][ci];
          const ratio = grid.max ? n / grid.max : 0;
          return (
            <div key={wd} className="flex-1 border border-line/60" style={{ minHeight: 20 }}
              title={`${DAYS[wd]} ${h}시 · ${n}건`}>
              <div className="h-full w-full" style={{ backgroundColor: "var(--color-primary)", opacity: n === 0 ? 0 : 0.12 + ratio * 0.68 }} />
            </div>
          );
        })}
      </div>
    ))}
  </div>
</div>
<p className="mt-2 text-[10px] leading-relaxed text-muted">
  진하게 = 예약 많음 · 빈칸 = 예약 없는 시간대. ※ 트레이너 정원(근무시간) 데이터가 없어 '가동률 %'가 아니라 예약 <b>밀도</b>예요.
</p>
```
> `grid.total === 0`이면 히트맵 대신 "최근 90일 예약이 없습니다." 빈상태. 불투명도는 `0.12~0.80` 램프(밀도 0=투명). inline `style`이라 정적-리터럴 purge 규율 무관(클래스 아님).

### 4-4. 블록③ 트레이너별 스케줄 표
- 컬럼: 트레이너 · 예약 · 완료 · 취소 · 미처리 · 완료율 · 노쇼율. (ConversionFunnel 표 패턴 · `min-w-[640px]` overflow-x-auto)
- 완료율: `text-cyan-700`(≥80%)·`text-danger-text`(<50%)·`text-ink`(그외)·`text-muted`(표본0). 노쇼율: `text-danger-text`(>15%)·`text-ink`(그외)·`text-muted`(표본0).
- 행 = `trainers` 순회(모든 트레이너 표시 · outcome/noshow는 `.find(trainer_id)`로 조인 · 없으면 0/—). 데이터 원천이 둘(appts=outcome / logs=noshow)이라 트레이너 id로 병합.
```jsx
{trainers.map((t) => {
  const o = outcome.find((x) => x.trainer_id === t.id) || { total:0, done:0, canceled:0, pastDue:0, doneRate:null };
  const ns = noshow.find((x) => x.trainer_id === t.id) || { noshowRate:null };
  // …td들: nameOf(t.id) · o.total · o.done · o.canceled · o.pastDue · pctText(o.doneRate) · pctText(ns.noshowRate)
})}
```
> 캡션: `완료율 = 완료 ÷ (완료+취소) · 노쇼율 = 수업일지 노쇼 ÷ 전체 수업(예약과 별개 집계).`

### 4-5. 블록④ 미처리 예약 리스트 (정리 필요)
- `ToneCard tone="danger"`(또는 unclosed) — 시간 지났는데 booked인 예약. 회원명·트레이너·날짜시간. 상위 LIMIT + "외 N건".
```jsx
<ToneCard tone="danger">
  <div className="mb-2 flex items-center gap-2">
    <AlertTriangle className="h-4 w-4 text-danger-text" />
    <h3 className="text-sm font-bold text-ink">미처리 예약 · 완료/취소 안 됨</h3>
    <span className="rounded-full bg-rose-500/15 px-2 py-0.5 text-[10px] font-bold text-danger-text">{pastDue.length}</span>
  </div>
  {pastDue.length === 0 ? (
    <p className="text-[12px] text-muted">미처리 예약이 없습니다.</p>
  ) : (
    <ul className="space-y-1.5">
      {pastDue.slice(0, LIMIT).map((a) => (
        <li key={a.id} className="flex flex-wrap items-center gap-x-2 gap-y-1 rounded-xl border border-rose-500/20 bg-card px-3 py-2.5">
          <span className="text-sm font-semibold text-ink">{memberName(a.user_id)}</span>
          <span className="text-[11px] text-muted">{nameOf(a.trainer_id)}</span>
          <span className="ml-auto font-mono text-[11px] text-muted">{a.start_at.slice(5,10).replace("-","/")} {a.start_at.slice(11,16)}<span className="ml-1 text-[10px]">(UTC저장)</span></span>
        </li>
      ))}
    </ul>
  )}
  <p className="mt-2 text-[10px] leading-relaxed text-muted">지난 예약을 완료(차감)나 취소로 정리하면 통계가 정확해져요.</p>
</ToneCard>
```
> ⚠️ **시간 표기 주의:** `start_at`은 UTC ISO. 리스트에 시:분을 그대로 자르면 UTC라 9시간 어긋난다. **KST로 보정해 표기**: `const k = new Date(Date.parse(a.start_at)+9*3600*1000); ${k.getUTCMonth()+1}/${k.getUTCDate()} ${String(k.getUTCHours()).padStart(2,"0")}:${String(k.getUTCMinutes()).padStart(2,"0")}` (히트맵과 같은 KST 규칙). 위 코드의 `slice`는 **의사코드** — 실제론 KST 보정 헬퍼로 표기(위 "(UTC저장)" 꼬리표 대신 KST 시각). 히트맵과 시간축이 어긋나면 안 됨.

전체 레이아웃: `<div className="space-y-6">` — 역할 1줄 → 요약 타일 grid → 히트맵 Card → 트레이너 표 Card → 미처리 ToneCard.

---

## 5. 색·워딩 규율
- **emerald 금지.** 밀도 = primary 불투명도(inline style) · 좋음(완료율↑) = cyan · 주의(노쇼↑·미처리) = rose(danger). 표/타일 텍스트 색은 정적 리터럴.
- **정직 캡션 2개 필수:** ① "가동률 %가 아니라 예약 밀도(정원 데이터 없음)" ② "노쇼는 수업일지 기준(예약과 별개)". 허위 정밀도 금지.
- **쉬운말:** "예약이 언제 몰리나·완료율·노쇼율·미처리 예약". "가동률·슬롯" 은어는 캡션에서 풀어 씀.
- 표본0 = "—"·빈상태. no-data ≠ 주의.
- **KST 일관:** 히트맵·미처리 리스트 모두 `start_at(UTC)`를 +9h 보정해 요일/시각 판정(어긋남 금지).

---

## 6. 검증 (구현 후)
- **웹Claude 실행검증:** 배치1(memberStatus 3함수)을 스테이징 원본에 붙여 토이 데이터로 로컬 실행 — **완료(§2 로그).** KST 요일/시간 경계·완료율·노쇼율(voided 제외)·미처리·빈데이터 통과. CC diff 오면 코드 대조 재확인.
- `npm.cmd run build`/`lint` green(신규 경고 0 · 미사용 import 0 · 색 동적 클래스 조립 0 · inline style은 허용).
- **폰 확인:** 스케줄 탭 렌더 · 히트맵 요일축(월~일)·시간축·밀도 진하기·빈칸 · peak 라벨 · 트레이너 표 완료율(cyan/rose)·노쇼율 · 미처리 리스트 **시각이 KST**(주간 스케줄 보드와 같은 시간) · 표본0 "—".
- **정합성:** 히트맵 `byWeekday`·`byHour` 합 = `total` · 미처리 리스트 건수 = 트레이너표 pastDue 합 = 요약 타일 미처리수 · 노쇼율 원천이 리텐션/실적탭 수업집계(`source!=='noshow'`)와 같은 predicate.
- appointment fetch 실패해도 페이지 안 죽고 스케줄 탭만 빈상태(비차단 확인).

---

## 7. 커밋 배치 (배치별 1커밋 = revert 단위)
1. `feat(stats): 가동률·스케줄 파생 3종 추가` — `lib/memberStatus.js`
   - `git commit -m "feat(stats): 가동률·스케줄 파생 3종 추가(apptWeekdayHourGrid·apptOutcomeByTrainer·noshowByTrainer)" -- lib/memberStatus.js`
2. `feat(admin): 가동률·스케줄 탭(#5) + appointment 배선` — `components/admin/ScheduleAnalytics.jsx`(신규 · **`git add` 먼저**) + `app/admin/page.jsx`(ATABS '스케줄' · appts fetch(90일·비차단)·state · 섹션)
   - `git add components/admin/ScheduleAnalytics.jsx`
   - `git commit -m "feat(admin): 가동률·스케줄 탭(#5 밀도 히트맵·트레이너 완료/노쇼·미처리)" -- components/admin/ScheduleAnalytics.jsx app/admin/page.jsx`

> 배치1(순수함수)만 먼저 올려도 무해(콜사이트 0). 진짜 가동률 %(정원 스키마)는 이번 커밋에 없음(별도 스프린트 · 결정 시).

---

## 8. 다음 작업 큐 (참고)
- **#6 AI 원장 브리핑** — #1~#5 파생을 임팩트순 3개로(룰 기반 먼저 · 무료·결정적). 만료임박(예상매출)·클로징임박·실적급락·노쇼급증·미처리예약.
- **빈상태 처리(즉효)** — 베타 첫날 온보딩 빈상태(#1~#5 각 탭).
- (선택) 진짜 가동률 — 트레이너 근무시간/주간정원 테이블 + 입력 UI → `apptWeekdayHourGrid` 위에 분모 얹어 % 화면.
